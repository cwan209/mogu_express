# Sprint 1.1 — 数据 schema 扩展 + OAuth-only 身份清理 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 Batch 1 P0 数据模型变化(群号/订单 tracking/卖家备注/商品扩展),并完全清除 OTP 代码,把身份模型重构为 OAuth-only。

**Architecture:**
- 后端云函数 `wxLogin / registerProfile / createOrder / login` 改造,2 个新 admin cf (`updateTracking` / `updateOrderNotes`),sendOtp/verifyOtp 删除。
- mongo schemaless,无 migration script;旧 `order.remark` / `user.name|phone` 字段不删,新代码不读写。
- 前端 web-shop 简化登录流程为 OAuth-only,RegisterProfile 单字段(群号);web-admin OrderDetail/ProductEdit/TuanEdit 加新 UI。
- 测试:`local-backend/api/test-shim.js` 集成测试(纯内存 mongo mock,沿用现有 pattern)。

**Tech Stack:** Node.js (wx-server-sdk shim) / MongoDB / React + Vite + antd-mobile (web-shop) / React + Vite + antd (web-admin) / Zustand。

**Spec 来源:** `docs/superpowers/specs/2026-05-18-sprint1.1-data-schema-oauth-cleanup-design.md`

---

## File Structure(改动总览)

### 后端云函数

| 文件 | 动作 |
|---|---|
| `cloudfunctions/sendOtp/` | 删除整目录 |
| `cloudfunctions/verifyOtp/` | 删除整目录 |
| `cloudfunctions/login/index.js` | 改 `userInfo` 返回加 `groupId` |
| `cloudfunctions/registerProfile/index.js` | 简化:入参 `{groupId}` |
| `cloudfunctions/createOrder/index.js` | 去 name/phone check + userSnapshot 重定义 + notes.buyer |
| `cloudfunctions/wxLogin/index.js` | `isRegistered = Boolean(u.groupId)` |
| `cloudfunctions/_admin/productCRUD/index.js` | 接 7 新字段 |
| `cloudfunctions/_admin/updateTracking/` | 新增 cf |
| `cloudfunctions/_admin/updateOrderNotes/` | 新增 cf |

### 前端 web-shop

| 文件 | 动作 |
|---|---|
| `web-shop/src/types/index.ts` | Order 加 notes/tracking,userSnapshot 类型重定义 |
| `web-shop/src/store/auth.ts` | UserProfile 加 `groupId?: string` |
| `web-shop/src/api/auth.ts` | 删 sendOtp/verifyOtp,registerProfile 入参改 |
| `web-shop/src/pages/RegisterProfile.tsx` | 重写:单字段群号 |
| `web-shop/src/pages/Login.tsx` | 删除 |
| `web-shop/src/App.tsx` | 删 `/login` 路由,Protected 跳 `/qr-fallback` |
| `web-shop/src/pages/OrderDetail.tsx` | 加物流 Card + 团长留言显示 |

### 前端 web-admin

| 文件 | 动作 |
|---|---|
| `web-admin/src/types.ts` | Order 加 notes/tracking/userSnapshot,Product 加 7 字段 |
| `web-admin/src/api/order.ts` | `updateTracking` + `updateOrderNotes` callers |
| `web-admin/src/pages/OrderDetail.tsx` | 3 个 Card(物流/卖家备注/客户群号显示) |
| `web-admin/src/pages/ProductEdit.tsx` | 加 7 字段 input |
| `web-admin/src/pages/TuanEdit.tsx` | 选 product 时自动 fill 联动 |

### 测试 / 配置

| 文件 | 动作 |
|---|---|
| `local-backend/api/test-shim.js` | 改 fixtures,删 2-3 旧 test,加 ~6 新 test |
| `deploy/docker-compose.production.yml` | 删 `SMS_STUB` / `TENCENT_SMS_*` / `BUMP_EXPIRED_TUANS` env |

---

## Phase A — 后端云函数改造(7 tasks)

### Task A1: registerProfile 简化 + login cf userInfo 加 groupId

**Files:**
- Modify: `cloudfunctions/registerProfile/index.js`
- Modify: `cloudfunctions/login/index.js:68-77`
- Modify: `local-backend/api/test-shim.js:222-230`(改既有测试)

- [ ] **Step 1: 改 test 'registerProfile 后 isRegistered 变 true'**

`local-backend/api/test-shim.js` 找到 line 222,改为:

```js
test('registerProfile 后 isRegistered 变 true (groupId 流程)', async () => {
  reset();
  shim.__setContext({ OPENID: 'u_new' });
  await requireCf('login').main({}, {});
  await requireCf('registerProfile').main({ groupId: '墨尔本生鲜三号群' }, {});
  const r2 = await requireCf('login').main({}, {});
  assert.equal(r2.isRegistered, true);
  assert.equal(r2.userInfo.groupId, '墨尔本生鲜三号群');
});

test('registerProfile 空 groupId 拒', async () => {
  reset();
  shim.__setContext({ OPENID: 'u_new' });
  await requireCf('login').main({}, {});
  const r = await requireCf('registerProfile').main({}, {});
  assert.equal(r.code, 1);
  assert.match(r.message, /groupId/);
});
```

- [ ] **Step 2: 跑 test 验证 fail**

```bash
node local-backend/api/test-shim.js 2>&1 | grep -E '✗|passed'
```

Expected: FAIL on `groupId` test (cf still requires name/phone) + FAIL on register success test (userInfo.groupId undefined)

- [ ] **Step 3: 改 registerProfile cf**

`cloudfunctions/registerProfile/index.js` 整文件 replace:

```js
// registerProfile - 完善用户资料(群号)
const cloud = require('wx-server-sdk');
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });
const db = cloud.database();

exports.main = async (event) => {
  const { OPENID } = cloud.getWXContext();
  if (!OPENID) return { code: 401, message: 'not logged in' };
  const { groupId } = event || {};
  if (!groupId || typeof groupId !== 'string' || !groupId.trim()) {
    return { code: 1, message: 'groupId required' };
  }
  const trimmed = groupId.trim().slice(0, 30);

  const now = new Date();
  const col = db.collection('users');
  const existing = await col.where({ _openid: OPENID }).limit(1).get();
  if (existing.data && existing.data.length) {
    await col.doc(existing.data[0]._id).update({
      data: { groupId: trimmed, registeredAt: existing.data[0].registeredAt || now, updatedAt: now },
    });
  } else {
    await col.add({
      data: { _openid: OPENID, groupId: trimmed, registeredAt: now, createdAt: now, updatedAt: now },
    });
  }
  return { code: 0, groupId: trimmed };
};
```

- [ ] **Step 4: 改 login cf userInfo 返回**

`cloudfunctions/login/index.js` line 73-76,替换 userInfo block:

```js
    userInfo: {
      name: userDoc.name || '',       // legacy 兼容,新代码不用
      phone: userDoc.phone || '',     // legacy 兼容
      groupId: userDoc.groupId || '',
      wechat: userDoc.wechat || null,
    },
```

- [ ] **Step 5: 跑 test 验证 pass**

```bash
node local-backend/api/test-shim.js 2>&1 | tail -5
```

Expected: `41 passed` (or close — depending on existing count after our changes)

- [ ] **Step 6: Commit**

```bash
git add cloudfunctions/registerProfile/ cloudfunctions/login/ local-backend/api/test-shim.js
git commit -m "feat(register): 简化为 groupId 单字段,login cf userInfo 加 groupId

registerProfile 入参从 {name, phone, address?} → {groupId},
非空+trim+30 字符 cap。

login cf 的 userInfo 返回加 groupId + wechat,name/phone 保留 legacy
兼容(老用户文档读取不报错)。

test-shim.js:
- 改 'registerProfile 后 isRegistered 变 true' 用 groupId
- 加 'registerProfile 空 groupId 拒'
- isRegistered 仍走 \`!!userDoc.registeredAt\`(无需改 login 逻辑)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task A2: createOrder userSnapshot 重定义 + notes.buyer + 去 name/phone check

**Files:**
- Modify: `cloudfunctions/createOrder/index.js:43-118`(用户检查、userSnapshot 构造、remark→notes)
- Modify: `local-backend/api/test-shim.js`(fixtures 改 wechat+groupId,加 userSnapshot 新形态测试)

- [ ] **Step 1: 改 createOrder fixtures + 加新测试**

`local-backend/api/test-shim.js` 找 line 240 附近的 createOrder 测试,把 users fixtures 改成 OAuth 形态。具体替换:

找到所有出现:
```js
{ _openid: 'u1', name: 'A', phone: '0400', registeredAt: new Date() }
```

替换为:
```js
{ _openid: 'u1', wechat: { nickname: 'A', avatar: 'https://x.png', sex: 0, country: null, province: null, city: null, language: null }, groupId: 'g1', registeredAt: new Date() }
```

(可能需要改 4-5 处,在 createOrder/cancelOrder/simulatePay/refund 等测试里)

文件末尾(在 addressValidate 测试之前)加 2 个新测试:

```js
test('createOrder userSnapshot 新形态 (nickname/avatar/groupId)', async () => {
  reset({
    users: [{ _openid: 'u1', wechat: { nickname: 'Luke', avatar: 'http://a.png', sex: 1, country: 'Australia', province: 'VIC', city: 'Melbourne', language: 'en' }, groupId: 'g1', registeredAt: new Date() }],
    addresses: [{ _id: 'addr1', _openid: 'u1', isDefault: true,
      recipient: 'A', phone: '0400', line1: '1 St', line2: '',
      suburb: 'M', state: 'VIC', postcode: '3000' }],
  });
  shim.__setContext({ OPENID: 'u1' });
  const r = await requireCf('createOrder').main({
    items: [{ tuanItemId: 'ti_p1_tuan_001', quantity: 1 }],
    addressId: 'addr1', requirePay: false,
  }, {});
  assert.equal(r.code, 0);
  assert.equal(r.order.userSnapshot.nickname, 'Luke');
  assert.equal(r.order.userSnapshot.avatar, 'http://a.png');
  assert.equal(r.order.userSnapshot.groupId, 'g1');
  assert.equal(r.order.userSnapshot.name, undefined, 'name should not be written');
  assert.equal(r.order.userSnapshot.phone, undefined, 'phone should not be written');
});

test('createOrder remark → notes.buyer', async () => {
  reset({
    users: [{ _openid: 'u1', wechat: { nickname: 'A' }, groupId: 'g1', registeredAt: new Date() }],
    addresses: [{ _id: 'addr1', _openid: 'u1', isDefault: true,
      recipient: 'A', phone: '0400', line1: '1 St', line2: '',
      suburb: 'M', state: 'VIC', postcode: '3000' }],
  });
  shim.__setContext({ OPENID: 'u1' });
  const r = await requireCf('createOrder').main({
    items: [{ tuanItemId: 'ti_p1_tuan_001', quantity: 1 }],
    addressId: 'addr1', remark: '留门卫', requirePay: false,
  }, {});
  assert.equal(r.code, 0);
  assert.deepEqual(r.order.notes, { buyer: '留门卫', seller: '' });
  assert.equal(r.order.remark, undefined, 'remark field no longer written');
});

test('createOrder 无 name/phone 也能下单 (OAuth 用户)', async () => {
  reset({
    users: [{ _openid: 'u1', wechat: { nickname: 'A' }, groupId: 'g1', registeredAt: new Date() }],
    // 注意:user 没 name/phone 字段
    addresses: [{ _id: 'addr1', _openid: 'u1', isDefault: true,
      recipient: 'A', phone: '0400', line1: '1 St', line2: '',
      suburb: 'M', state: 'VIC', postcode: '3000' }],
  });
  shim.__setContext({ OPENID: 'u1' });
  const r = await requireCf('createOrder').main({
    items: [{ tuanItemId: 'ti_p1_tuan_001', quantity: 1 }],
    addressId: 'addr1', requirePay: false,
  }, {});
  assert.equal(r.code, 0, `expected success, got: ${JSON.stringify(r)}`);
});
```

- [ ] **Step 2: 跑 test 验证 fail**

```bash
node local-backend/api/test-shim.js 2>&1 | grep -E '✗|passed' | head
```

Expected: 上述 3 个新测试 + 几个老 createOrder 测试 fail(因为 fixture 改了但 cf 还在读 user.name/phone)

- [ ] **Step 3: 改 createOrder cf — 去 name/phone check**

`cloudfunctions/createOrder/index.js` line 49-50,替换:

```js
  const user = userRes.data && userRes.data[0];
  if (!user || !user.name || !user.phone) return { code: 2, message: '请先完善姓名电话' };
```

改成:
```js
  const user = userRes.data && userRes.data[0];
  if (!user) return { code: 401, message: 'not logged in' };
```

- [ ] **Step 4: 改 createOrder cf — userSnapshot 重定义**

`cloudfunctions/createOrder/index.js` 找到 line 106 附近的 `userSnapshot`:

```js
      userSnapshot: { name: user.name, phone: user.phone },
```

替换:
```js
      userSnapshot: {
        nickname: user.wechat?.nickname || '微信用户',
        avatar: user.wechat?.avatar || null,
        groupId: user.groupId || null,
      },
```

- [ ] **Step 5: 改 createOrder cf — remark 写到 notes.buyer**

`cloudfunctions/createOrder/index.js` 找到 line 118 附近:

```js
      remark: remark || '',
```

替换:
```js
      notes: { buyer: remark || '', seller: '' },
```

(把 `remark:` key 直接删掉,改成 `notes` 子文档)

- [ ] **Step 6: 跑 test 验证 pass**

```bash
node local-backend/api/test-shim.js 2>&1 | tail -5
```

Expected: all pass

- [ ] **Step 7: Commit**

```bash
git add cloudfunctions/createOrder/index.js local-backend/api/test-shim.js
git commit -m "refactor(createOrder): OAuth-only userSnapshot + notes.buyer

变化:
- 去掉 \`!user.name || !user.phone\` 拒单检查,OAuth 用户能下单
- userSnapshot 从 {name, phone} 重定义为 {nickname, avatar, groupId}
  (从 user.wechat + user.groupId 拿)
- remark 字段 → notes: {buyer, seller}
  (用户端读取 fallback: order.notes?.buyer ?? order.remark,兼容老订单)

test-shim.js fixtures 全部 user.name/phone → user.wechat/groupId,
加 3 个新测试覆盖新 userSnapshot/notes/OAuth 用户下单。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task A3: wxLogin isRegistered = Boolean(groupId)

**Files:**
- Modify: `cloudfunctions/wxLogin/index.js`(单行改动)

- [ ] **Step 1: 改 wxLogin**

`cloudfunctions/wxLogin/index.js` 找到 `isRegistered = Boolean(u.name)`:

```bash
grep -n "isRegistered = Boolean" cloudfunctions/wxLogin/index.js
```

替换 `Boolean(u.name)` → `Boolean(u.groupId)`:

```js
    isRegistered = Boolean(u.groupId);
```

- [ ] **Step 2: 手动 sanity check**

```bash
node -e "
const fs = require('fs');
const src = fs.readFileSync('cloudfunctions/wxLogin/index.js', 'utf8');
console.log(src.includes('Boolean(u.groupId)') ? 'OK' : 'NOT updated');
"
```

Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add cloudfunctions/wxLogin/index.js
git commit -m "fix(wxLogin): isRegistered 判断改用 groupId 替代 name

OAuth-only 流程下 user.name 永远空,改用 groupId 判断是否完成首次注册。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task A4: 新增 _admin/updateTracking cf

**Files:**
- Create: `cloudfunctions/_admin/updateTracking/index.js`
- Create: `cloudfunctions/_admin/updateTracking/package.json`
- Create: `cloudfunctions/_admin/updateTracking/jwt.js`(copy from sibling)
- Create: `cloudfunctions/_admin/updateTracking/config.json`
- Modify: `local-backend/api/test-shim.js`(加 1 个新测试)

- [ ] **Step 1: 加 test**

文件末尾 addressValidate 测试之前,加:

```js
test('_admin/updateTracking 写入 order.tracking', async () => {
  reset({
    orders: [{
      _id: 'order_x', _openid: 'u1', orderNo: 'MG_X',
      items: [], amount: 0, status: 'paid', payStatus: 'paid',
      shipping: {}, createdAt: new Date(),
    }],
    admins: [{ openid: 'admin1', username: 'admin' }],
  });
  shim.__setContext({ OPENID: 'admin1' });
  const r = await requireCf('_admin/updateTracking').main({
    orderId: 'order_x',
    weight: 2.5,
    courierName: '顺丰',
    courierNo: 'SF1234567890',
  }, {});
  assert.equal(r.code, 0);
  // 验证 mongo 写入
  const orderRes = await mockClient.db('mogu_express').collection('orders').findOne({ _id: 'order_x' });
  assert.equal(orderRes.tracking.weight, 2.5);
  assert.equal(orderRes.tracking.courierName, '顺丰');
  assert.equal(orderRes.tracking.courierNo, 'SF1234567890');
});

test('_admin/updateTracking 非 admin 拒', async () => {
  reset({
    orders: [{ _id: 'order_x', _openid: 'u1', orderNo: 'MG_X', items: [], amount: 0, status: 'paid', payStatus: 'paid', shipping: {}, createdAt: new Date() }],
    admins: [],
  });
  shim.__setContext({ OPENID: 'regular_user' });
  const r = await requireCf('_admin/updateTracking').main({
    orderId: 'order_x', weight: 1, courierName: '顺丰', courierNo: 'X',
  }, {});
  assert.equal(r.code, 403);
});
```

- [ ] **Step 2: 跑 test 验证 fail**

```bash
node local-backend/api/test-shim.js 2>&1 | grep updateTracking
```

Expected: `updateTracking` test fails(cf 不存在)

- [ ] **Step 3: 创建 cf**

```bash
mkdir -p cloudfunctions/_admin/updateTracking
cp cloudfunctions/_admin/setShippingFee/jwt.js cloudfunctions/_admin/updateTracking/jwt.js
```

`cloudfunctions/_admin/updateTracking/package.json`:
```json
{
  "name": "_admin_updateTracking",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": { "wx-server-sdk": "~2.6.3" }
}
```

`cloudfunctions/_admin/updateTracking/config.json`:
```json
{ "permissions": { "openapi": [] } }
```

`cloudfunctions/_admin/updateTracking/index.js`:
```js
// _admin/updateTracking — 团长 admin 设/改订单物流信息
//
// 入参: { orderId, weight?, courierName?, courierNo? }
// 出参: { code: 0, orderId, tracking } 或错误码

const cloud = require('wx-server-sdk');
const { verify } = require('./jwt.js');

cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });
const db = cloud.database();

const JWT_SECRET = process.env.JWT_SECRET || 'mogu_express_dev_secret_REPLACE_ME_IN_PROD';

const COURIER_ENUM = ['顺丰', '中通', '圆通', '极兔', 'EMS', 'Australia Post', 'StarTrack', '其他'];

async function requireAdmin(event) {
  if (event && event.token) {
    try { return { source: 'web', admin: verify(event.token, JWT_SECRET) }; }
    catch (err) { const e = new Error('invalid token'); e.code = 401; throw e; }
  }
  const { OPENID } = cloud.getWXContext();
  if (!OPENID) { const e = new Error('no openid'); e.code = 401; throw e; }
  const res = await db.collection('admins').where({ openid: OPENID }).limit(1).get();
  if (!res.data || !res.data.length) { const e = new Error('not admin'); e.code = 403; throw e; }
  return { source: 'mp', admin: res.data[0] };
}

exports.main = async (event) => {
  try {
    await requireAdmin(event);
    const { orderId, weight, courierName, courierNo } = event || {};
    if (!orderId) return { code: 1, message: 'orderId required' };

    if (weight !== undefined && weight !== null) {
      if (typeof weight !== 'number' || !Number.isFinite(weight) || weight < 0 || weight > 1000) {
        return { code: 2, message: 'weight must be 0..1000 kg' };
      }
    }
    if (courierName !== undefined && courierName !== null && courierName !== '') {
      if (!COURIER_ENUM.includes(courierName)) {
        return { code: 3, message: `courierName not in enum: ${COURIER_ENUM.join('/')}` };
      }
    }
    if (courierNo !== undefined && courierNo !== null) {
      if (typeof courierNo !== 'string' || courierNo.length > 100) {
        return { code: 4, message: 'courierNo too long' };
      }
    }

    const col = db.collection('orders');
    const doc = await col.doc(orderId).get().catch(() => null);
    if (!doc || !doc.data) return { code: 5, message: 'order not found' };

    const now = new Date();
    const tracking = {
      weight: weight ?? null,
      courierName: courierName || null,
      courierNo: courierNo || null,
      setAt: now,
    };

    await col.doc(orderId).update({
      data: { tracking, updatedAt: now },
    });

    return { code: 0, orderId, tracking };
  } catch (err) {
    return { code: err.code || 500, message: err.message || 'error' };
  }
};
```

- [ ] **Step 4: 跑 test 验证 pass**

```bash
node local-backend/api/test-shim.js 2>&1 | grep -E 'updateTracking|passed' | tail -5
```

Expected: `updateTracking` tests pass, total `4N passed`

- [ ] **Step 5: Commit**

```bash
git add cloudfunctions/_admin/updateTracking/ local-backend/api/test-shim.js
git commit -m "feat(_admin): updateTracking 云函数

团长 admin 设/改订单物流(重量 + 快递公司 + 单号)。
courierName enum 校验,weight 0..1000 kg,courierNo <100 字符。

写入 order.tracking 子文档,跟 shippingFee 平行。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task A5: 新增 _admin/updateOrderNotes cf

**Files:**
- Create: `cloudfunctions/_admin/updateOrderNotes/index.js` + package.json + jwt.js + config.json
- Modify: `local-backend/api/test-shim.js`(加 1-2 个测试)

- [ ] **Step 1: 加 test**

文件末尾加:

```js
test('_admin/updateOrderNotes 写入 order.notes.seller', async () => {
  reset({
    orders: [{
      _id: 'order_x', _openid: 'u1', orderNo: 'MG_X',
      items: [], amount: 0, status: 'paid', payStatus: 'paid',
      shipping: {}, notes: { buyer: '原买家备注', seller: '' },
      createdAt: new Date(),
    }],
    admins: [{ openid: 'admin1', username: 'admin' }],
  });
  shim.__setContext({ OPENID: 'admin1' });
  const r = await requireCf('_admin/updateOrderNotes').main({
    orderId: 'order_x',
    sellerNote: '明天发货,VIP',
  }, {});
  assert.equal(r.code, 0);
  const orderRes = await mockClient.db('mogu_express').collection('orders').findOne({ _id: 'order_x' });
  assert.equal(orderRes.notes.seller, '明天发货,VIP');
  assert.equal(orderRes.notes.buyer, '原买家备注', 'buyer note unchanged');
});

test('_admin/updateOrderNotes 超 500 字拒', async () => {
  reset({
    orders: [{ _id: 'order_x', _openid: 'u1', orderNo: 'MG_X', items: [], amount: 0, status: 'paid', payStatus: 'paid', shipping: {}, createdAt: new Date() }],
    admins: [{ openid: 'admin1', username: 'admin' }],
  });
  shim.__setContext({ OPENID: 'admin1' });
  const longText = 'x'.repeat(501);
  const r = await requireCf('_admin/updateOrderNotes').main({
    orderId: 'order_x', sellerNote: longText,
  }, {});
  assert.equal(r.code, 2);
});
```

- [ ] **Step 2: 跑 test 验证 fail**

```bash
node local-backend/api/test-shim.js 2>&1 | grep updateOrderNotes
```

Expected: 2 tests fail

- [ ] **Step 3: 创建 cf**

```bash
mkdir -p cloudfunctions/_admin/updateOrderNotes
cp cloudfunctions/_admin/setShippingFee/jwt.js cloudfunctions/_admin/updateOrderNotes/jwt.js
```

`package.json`:
```json
{
  "name": "_admin_updateOrderNotes",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": { "wx-server-sdk": "~2.6.3" }
}
```

`config.json`:
```json
{ "permissions": { "openapi": [] } }
```

`index.js`:
```js
// _admin/updateOrderNotes — admin 设/改订单卖家备注
//
// 入参: { orderId, sellerNote }
// 出参: { code: 0, orderId, notes } 或错误码
//
// 只动 notes.seller,不影响 notes.buyer。
// 兼容老订单:若 notes 不存在,初始化为 {buyer:'', seller:sellerNote}。

const cloud = require('wx-server-sdk');
const { verify } = require('./jwt.js');

cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });
const db = cloud.database();

const JWT_SECRET = process.env.JWT_SECRET || 'mogu_express_dev_secret_REPLACE_ME_IN_PROD';

async function requireAdmin(event) {
  if (event && event.token) {
    try { return { source: 'web', admin: verify(event.token, JWT_SECRET) }; }
    catch (err) { const e = new Error('invalid token'); e.code = 401; throw e; }
  }
  const { OPENID } = cloud.getWXContext();
  if (!OPENID) { const e = new Error('no openid'); e.code = 401; throw e; }
  const res = await db.collection('admins').where({ openid: OPENID }).limit(1).get();
  if (!res.data || !res.data.length) { const e = new Error('not admin'); e.code = 403; throw e; }
  return { source: 'mp', admin: res.data[0] };
}

exports.main = async (event) => {
  try {
    await requireAdmin(event);
    const { orderId, sellerNote } = event || {};
    if (!orderId) return { code: 1, message: 'orderId required' };
    if (typeof sellerNote !== 'string') return { code: 2, message: 'sellerNote must be string' };
    if (sellerNote.length > 500) return { code: 2, message: 'sellerNote max 500 chars' };

    const col = db.collection('orders');
    const doc = await col.doc(orderId).get().catch(() => null);
    if (!doc || !doc.data) return { code: 3, message: 'order not found' };
    const order = doc.data;

    const newNotes = {
      buyer: order.notes?.buyer ?? order.remark ?? '', // 老订单 fallback
      seller: sellerNote,
    };

    await col.doc(orderId).update({
      data: { notes: newNotes, updatedAt: new Date() },
    });

    return { code: 0, orderId, notes: newNotes };
  } catch (err) {
    return { code: err.code || 500, message: err.message || 'error' };
  }
};
```

- [ ] **Step 4: 跑 test 验证 pass**

```bash
node local-backend/api/test-shim.js 2>&1 | tail -5
```

- [ ] **Step 5: Commit**

```bash
git add cloudfunctions/_admin/updateOrderNotes/ local-backend/api/test-shim.js
git commit -m "feat(_admin): updateOrderNotes 云函数

admin 设/改 order.notes.seller(团长内部 / 跟买家沟通的备注)。
500 字 cap。

兼容老订单:notes 不存在时 fallback 到 order.remark 当 buyer 备注,
新订单已在 createOrder 用 notes.buyer 写。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task A6: _admin/productCRUD 加 7 字段

**Files:**
- Modify: `cloudfunctions/_admin/productCRUD/index.js`(加字段接收 + 校验 + 写入)
- Modify: `local-backend/api/test-shim.js`(加 1 个测试)

- [ ] **Step 1: 看现有 productCRUD 结构**

```bash
cat cloudfunctions/_admin/productCRUD/index.js | head -100
```

观察现有 create / update 的字段处理 pattern(可能用 `ALLOWED_FIELDS` 数组或 explicit 列举)。

- [ ] **Step 2: 加 test**

文件末尾加(放在现有 productCRUD section 字段测试之后):

```js
test('_admin/productCRUD create 含扩展字段 (brand/spec/basePrice 等)', async () => {
  reset({ admins: [{ openid: 'admin1', username: 'admin' }] });
  shim.__setContext({ OPENID: 'admin1' });
  const r = await requireCf('_admin/productCRUD').main({
    action: 'create',
    data: {
      title: '澳洲牛肉 500g',
      brand: 'Coles',
      spec: '500g/盒',
      basePrice: 4500,
      englishName: 'Australian Beef 500g',
      courierName: '顺丰',
      courierFactor: 1.5,
      secondaryImages: [
        { url: 'http://x.png', caption: '正面' },
        { url: 'http://y.png', caption: '背面营养表' },
      ],
    },
  }, {});
  assert.equal(r.code, 0);
  assert.equal(r.product.brand, 'Coles');
  assert.equal(r.product.spec, '500g/盒');
  assert.equal(r.product.basePrice, 4500);
  assert.equal(r.product.englishName, 'Australian Beef 500g');
  assert.equal(r.product.courierName, '顺丰');
  assert.equal(r.product.courierFactor, 1.5);
  assert.equal(r.product.secondaryImages.length, 2);
});

test('_admin/productCRUD create courierFactor 越界拒', async () => {
  reset({ admins: [{ openid: 'admin1', username: 'admin' }] });
  shim.__setContext({ OPENID: 'admin1' });
  const r = await requireCf('_admin/productCRUD').main({
    action: 'create',
    data: { title: 'X', courierFactor: 99 },
  }, {});
  assert.notEqual(r.code, 0, `expected error for courierFactor=99, got: ${JSON.stringify(r)}`);
});
```

- [ ] **Step 3: 跑 test 验证 fail**

```bash
node local-backend/api/test-shim.js 2>&1 | grep productCRUD | tail -5
```

Expected: 2 new tests fail

- [ ] **Step 4: 改 productCRUD cf 加字段**

`cloudfunctions/_admin/productCRUD/index.js` 找到 create / update 的字段白名单,加 7 个:

```js
const PRODUCT_FIELDS = [
  'title', 'desc', 'coverFileId', 'categoryId', 'section', // 现有
  // 新增 7 字段
  'brand', 'spec', 'basePrice', 'englishName',
  'courierName', 'courierFactor', 'secondaryImages',
];
```

加校验(在原来 section 校验类似位置):

```js
const COURIER_ENUM = ['顺丰', '中通', '圆通', '极兔', 'EMS', 'Australia Post', 'StarTrack', '其他'];

function validateProductData(data) {
  if (data.brand !== undefined && (typeof data.brand !== 'string' || data.brand.length > 50)) {
    return 'brand must be string <= 50 chars';
  }
  if (data.spec !== undefined && (typeof data.spec !== 'string' || data.spec.length > 100)) {
    return 'spec must be string <= 100 chars';
  }
  if (data.basePrice !== undefined && (typeof data.basePrice !== 'number' || data.basePrice < 0)) {
    return 'basePrice must be number >= 0 (cents)';
  }
  if (data.englishName !== undefined && (typeof data.englishName !== 'string' || data.englishName.length > 200)) {
    return 'englishName must be string <= 200 chars';
  }
  if (data.courierName !== undefined && data.courierName !== '' && !COURIER_ENUM.includes(data.courierName)) {
    return `courierName not in enum: ${COURIER_ENUM.join('/')}`;
  }
  if (data.courierFactor !== undefined) {
    if (typeof data.courierFactor !== 'number' || data.courierFactor < 0 || data.courierFactor > 10) {
      return 'courierFactor must be number 0..10';
    }
  }
  if (data.secondaryImages !== undefined) {
    if (!Array.isArray(data.secondaryImages)) return 'secondaryImages must be array';
    for (const img of data.secondaryImages) {
      if (!img || typeof img.url !== 'string' || typeof img.caption !== 'string') {
        return 'secondaryImages items must have {url, caption}';
      }
    }
  }
  return null;
}
```

在 create / update 的 main flow,filter event.data 用 PRODUCT_FIELDS 白名单,调 `validateProductData` 校验,然后写入 mongo。具体调用位置 follow现有 section 字段的处理风格。

- [ ] **Step 5: 跑 test 验证 pass**

```bash
node local-backend/api/test-shim.js 2>&1 | tail -5
```

- [ ] **Step 6: Commit**

```bash
git add cloudfunctions/_admin/productCRUD/index.js local-backend/api/test-shim.js
git commit -m "feat(_admin/productCRUD): 加 7 个商品扩展字段

brand, spec, basePrice (分), englishName, courierName (enum),
courierFactor (0-10), secondaryImages ([{url, caption}])。

枚举/范围/长度校验在 validateProductData。

供 tuan_item 创建时联动 fill 默认值 (admin TuanEdit Task D4)。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase B — 删除 OTP 云函数(1 task)

### Task B1: 删 sendOtp + verifyOtp

**Files:**
- Delete: `cloudfunctions/sendOtp/`(整目录)
- Delete: `cloudfunctions/verifyOtp/`(整目录)

- [ ] **Step 1: 删除目录**

```bash
git rm -r cloudfunctions/sendOtp/ cloudfunctions/verifyOtp/
```

- [ ] **Step 2: 跑 test 确认不破坏**

```bash
node local-backend/api/test-shim.js 2>&1 | tail -5
```

Expected: all pass(没有依赖 OTP cf 的测试,前面 task 已 verify)

- [ ] **Step 3: Commit**

```bash
git commit -m "chore: 删 sendOtp / verifyOtp 云函数 (OAuth-only 之后不再需要)

身份模型已切到 wxLogin (OAuth code → JWT)。OTP 流程残留代码全删:
- cloudfunctions/sendOtp/ (含 sms.js)
- cloudfunctions/verifyOtp/ (jwt.js 副本在 wxLogin 已有)

前端 Login.tsx 和 sendOtp/verifyOtp api caller 在后续 Phase C 删。
mongo otp_codes collection 不动(部署后单独 drop 操作)。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase C — 前端 web-shop(5 tasks)

### Task C1: types + store 类型扩展

**Files:**
- Modify: `web-shop/src/types/index.ts`
- Modify: `web-shop/src/store/auth.ts`

- [ ] **Step 1: 改 types/index.ts**

读现有 `web-shop/src/types/index.ts` 找 `interface Order` 和 `userSnapshot` 类型定义。

`userSnapshot` 类型改成:
```ts
userSnapshot: {
  nickname?: string;
  avatar?: string | null;
  groupId?: string | null;
  // legacy(老订单可能有)
  name?: string;
  phone?: string;
};
```

Order 加新字段:
```ts
notes?: {
  buyer?: string;
  seller?: string;
};
tracking?: {
  weight?: number | null;
  courierName?: string | null;
  courierNo?: string | null;
  setAt?: string | null;
};
// remark 字段保留(legacy 显示用)
remark?: string;
```

- [ ] **Step 2: 改 store/auth.ts UserProfile**

`web-shop/src/store/auth.ts` 找到 `interface UserProfile`:

```ts
interface UserProfile {
  openid: string;
  name?: string;            // legacy,新代码不写
  phone?: string;           // legacy
  groupId?: string;         // 🆕 OAuth 用户的群号
  wechat?: WxProfile | null;
}
```

- [ ] **Step 3: tsc check**

```bash
cd web-shop && npx tsc --noEmit 2>&1 | head
```

Expected: 无 error

- [ ] **Step 4: Commit**

```bash
git add web-shop/src/types/index.ts web-shop/src/store/auth.ts
git commit -m "types(web-shop): Order 加 notes/tracking,UserProfile 加 groupId

身份模型迁移配套:
- Order 加 notes: {buyer, seller} + tracking: {weight, courier*}
- 保留 Order.remark 作 legacy 显示(老订单)
- UserProfile 加 groupId (OAuth 用户的群号)
- userSnapshot 类型新形态(nickname/avatar/groupId)+ 旧形态字段保留可选

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task C2: api/auth.ts 删 OTP + 改 registerProfile 签名

**Files:**
- Modify: `web-shop/src/api/auth.ts`

- [ ] **Step 1: 读现有 auth.ts**

```bash
cat web-shop/src/api/auth.ts
```

- [ ] **Step 2: 改写整文件**

```ts
import { callCloud } from './client';

export async function registerProfile(payload: { groupId: string }): Promise<{ code: 0 }> {
  return callCloud('registerProfile', payload);
}

export interface WxUserProfile {
  nickname: string | null;
  avatar: string | null;
  sex: 0 | 1 | 2 | null;
  country: string | null;
  province: string | null;
  city: string | null;
  language: string | null;
}

export interface WxLoginRes {
  code: 0;
  token: string;
  openid: string;
  isRegistered: boolean;
  user?: {
    name?: string;       // legacy
    phone?: string;      // legacy
    groupId?: string;
    wechat?: WxUserProfile | null;
  };
}

export async function wxLogin(code: string): Promise<WxLoginRes> {
  return callCloud<WxLoginRes>('wxLogin', { code });
}
```

(删除 SendOtpRes / VerifyOtpRes / sendOtp / verifyOtp,registerProfile 入参简化)

- [ ] **Step 3: 找 caller 报错处**

```bash
grep -rn "sendOtp\|verifyOtp" web-shop/src/ | grep -v "api/auth.ts\|test\|spec"
```

Expected: 应该只有 Login.tsx 用 sendOtp/verifyOtp。Login.tsx Task C4 整删,这里不动。

- [ ] **Step 4: tsc check**

```bash
cd web-shop && npx tsc --noEmit 2>&1 | head
```

Expected: 可能报 Login.tsx 引用 sendOtp/verifyOtp 错(因为还没删 Login.tsx)。Task C4 会删 Login.tsx,这里先**记录错误不阻塞**:

```bash
cd web-shop && npx tsc --noEmit 2>&1 | grep -v "Login.tsx" | head
```

应无 error。

- [ ] **Step 5: Commit**

```bash
git add web-shop/src/api/auth.ts
git commit -m "feat(api/auth): 删 sendOtp/verifyOtp,registerProfile 入参改 {groupId}

OAuth-only 切换。Login.tsx 的 caller 引用会暂时编译错,Task C4 整删。

registerProfile 入参 {name, phone, address?} → {groupId}。

WxLoginRes.user 加 groupId,保留 name/phone (legacy 字段)。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task C3: RegisterProfile.tsx 重写

**Files:**
- Modify: `web-shop/src/pages/RegisterProfile.tsx`(完全重写)

- [ ] **Step 1: 看现有 RegisterProfile.tsx 拿 layout 参考**

```bash
cat web-shop/src/pages/RegisterProfile.tsx | head -50
```

- [ ] **Step 2: 重写**

`web-shop/src/pages/RegisterProfile.tsx`:

```tsx
import { useState } from 'react';
import { useNavigate, useSearchParams } from 'react-router-dom';
import { NavBar, Input, Button, Toast, Form } from 'antd-mobile';
import { registerProfile } from '../api/auth';
import { useAuthStore } from '../store/auth';

export default function RegisterProfile() {
  const nav = useNavigate();
  const [params] = useSearchParams();
  const returnTo = params.get('returnTo') || '/';
  const user = useAuthStore((s) => s.user);
  const setRegistered = useAuthStore.setState;
  const updateUser = useAuthStore((s) => s.updateUser);

  const [groupId, setGroupId] = useState('');
  const [submitting, setSubmitting] = useState(false);

  const onSubmit = async () => {
    const trimmed = groupId.trim();
    if (!trimmed) {
      Toast.show({ icon: 'fail', content: '请填写群号' });
      return;
    }
    setSubmitting(true);
    try {
      await registerProfile({ groupId: trimmed });
      updateUser({ groupId: trimmed });
      setRegistered((s) => ({ ...s, isRegistered: true }));
      nav(returnTo, { replace: true });
    } catch (e: any) {
      Toast.show({ icon: 'fail', content: e.message || '提交失败' });
    } finally {
      setSubmitting(false);
    }
  };

  const nickname = user?.wechat?.nickname || '微信用户';

  return (
    <>
      <NavBar back={null}>完善资料</NavBar>
      <div style={{ padding: '24px 16px' }}>
        <p style={{ fontSize: 16, marginBottom: 8 }}>
          👋 看到您是 <strong>{nickname}</strong>
        </p>
        <p style={{ color: '#666', marginBottom: 24 }}>
          为了让团长找到您,请填写您所在的微信群号:
        </p>
        <Form layout="vertical">
          <Form.Item label="群号">
            <Input
              placeholder="如:墨尔本生鲜三号群"
              value={groupId}
              onChange={setGroupId}
              maxLength={30}
              clearable
            />
          </Form.Item>
        </Form>
        <Button
          block
          color="primary"
          loading={submitting}
          onClick={onSubmit}
          style={{ marginTop: 24 }}
        >
          完成
        </Button>
      </div>
    </>
  );
}
```

- [ ] **Step 3: tsc check**

```bash
cd web-shop && npx tsc --noEmit 2>&1 | grep -v "Login.tsx" | head
```

Expected: 无新增 error

- [ ] **Step 4: Commit**

```bash
git add web-shop/src/pages/RegisterProfile.tsx
git commit -m "feat(register): RegisterProfile 重写为单字段群号

OAuth 用户首次进商城弹此页填群号(替代原 name+phone)。
显示微信昵称给用户"亲切感",strip 干净 OTP 流程残留 UI。

submit 调 registerProfile({groupId}) → updateUser + setRegistered=true
→ 跳回 returnTo。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task C4: 删 Login.tsx + App.tsx 改路由

**Files:**
- Delete: `web-shop/src/pages/Login.tsx`
- Modify: `web-shop/src/App.tsx`(删 /login 路由,Protected 跳 /qr-fallback)

- [ ] **Step 1: 删 Login.tsx**

```bash
git rm web-shop/src/pages/Login.tsx
```

- [ ] **Step 2: 改 App.tsx**

读 `web-shop/src/App.tsx` 找:
1. `import Login from './pages/Login';` — 删
2. `<Route path="/login" element={<Login />} />` — 删
3. `Protected` 组件里 `<Navigate to="/login"` — 改 `to="/qr-fallback"`

具体:

```bash
grep -n "Login\|to=\"/login\"\|to='/login'" web-shop/src/App.tsx
```

执行 3 处修改。

- [ ] **Step 3: tsc check**

```bash
cd web-shop && npx tsc --noEmit 2>&1 | head
```

Expected: 无 error(此时 sendOtp/verifyOtp 引用全部清除了)

- [ ] **Step 4: Commit**

```bash
git add web-shop/src/pages/Login.tsx web-shop/src/App.tsx
git commit -m "feat(routing): 删 Login.tsx + /login 路由,Protected 跳 qr-fallback

OAuth-only 之后不再需要 OTP 登录页。
非微信入口的未登录用户 → /qr-fallback (扫码引导回微信)。
微信入口的未登录用户 → App.tsx bootstrap 已自动 redirect 到 OAuth。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task C5: 用户端 OrderDetail 加物流 Card + 团长留言

**Files:**
- Modify: `web-shop/src/pages/OrderDetail.tsx`

- [ ] **Step 1: 读现有用户端 OrderDetail**

```bash
cat web-shop/src/pages/OrderDetail.tsx | head -100
```

- [ ] **Step 2: 加 2 个 Card**

在订单基本信息 / 商品 / shippingFee Card 之后加两个 Card(具体位置 follow现有 antd-mobile Card 风格):

**物流信息 Card**:
```tsx
{order.tracking && (order.tracking.weight || order.tracking.courierNo) && (
  <div className="bg-white mt-2 p-4">
    <div className="text-xs text-gray-500 mb-2">物流信息</div>
    {order.tracking.weight != null && (
      <div className="text-sm mb-1">
        重量:{order.tracking.weight} kg
      </div>
    )}
    {order.tracking.courierName && (
      <div className="text-sm mb-1">
        快递:{order.tracking.courierName}
      </div>
    )}
    {order.tracking.courierNo && (
      <div className="flex items-center text-sm">
        <span>单号:{order.tracking.courierNo}</span>
        <Button
          size="mini"
          fill="none"
          onClick={() => {
            navigator.clipboard?.writeText(order.tracking!.courierNo!);
            Toast.show({ icon: 'success', content: '已复制' });
          }}
          style={{ marginLeft: 8 }}
        >
          复制
        </Button>
      </div>
    )}
  </div>
)}
```

**团长留言 Card**:
```tsx
{order.notes?.seller && (
  <div className="bg-white mt-2 p-4" style={{ background: '#FFFBE6' }}>
    <div className="text-xs text-gray-500 mb-2">📣 团长留言</div>
    <div className="text-sm">{order.notes.seller}</div>
  </div>
)}
```

确保有 import:`import { Toast, Button } from 'antd-mobile';`(若不全)

- [ ] **Step 3: tsc check**

```bash
cd web-shop && npx tsc --noEmit 2>&1 | head
```

- [ ] **Step 4: Commit**

```bash
git add web-shop/src/pages/OrderDetail.tsx
git commit -m "feat(web-shop/OrderDetail): 加物流信息 + 团长留言 Card

- 物流信息:重量 + 快递公司 + 单号(带复制按钮)
- 团长留言:order.notes.seller 显示,淡黄背景突出
- 字段不存在时 Card 整体隐藏

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase D — 前端 web-admin(4 tasks)

### Task D1: web-admin types + api callers

**Files:**
- Modify: `web-admin/src/types.ts`
- Modify: `web-admin/src/api/order.ts`
- Modify: `web-admin/src/api/product.ts`(若存在)

- [ ] **Step 1: 改 types.ts**

读现有 `web-admin/src/types.ts` 找 `interface Order` 和 `interface Product`。

加到 Order:
```ts
notes?: {
  buyer?: string;
  seller?: string;
};
tracking?: {
  weight?: number | null;
  courierName?: string | null;
  courierNo?: string | null;
  setAt?: string | null;
};
userSnapshot: {
  nickname?: string;
  avatar?: string | null;
  groupId?: string | null;
  name?: string;       // legacy
  phone?: string;      // legacy
};
```

加到 Product:
```ts
brand?: string;
spec?: string;
basePrice?: number;        // cents
englishName?: string;
courierName?: string;
courierFactor?: number;
secondaryImages?: Array<{ url: string; caption: string }>;
```

- [ ] **Step 2: 改 api/order.ts 加 callers**

末尾追加:

```ts
export async function updateTracking(orderId: string, data: {
  weight?: number;
  courierName?: string;
  courierNo?: string;
}) {
  return callCloud<{ code: 0; tracking: any }>('_admin/updateTracking', { orderId, ...data });
}

export async function updateOrderNotes(orderId: string, sellerNote: string) {
  return callCloud<{ code: 0; notes: any }>('_admin/updateOrderNotes', { orderId, sellerNote });
}
```

- [ ] **Step 3: tsc check**

```bash
cd web-admin && npx tsc --noEmit 2>&1 | head
```

- [ ] **Step 4: Commit**

```bash
git add web-admin/src/types.ts web-admin/src/api/order.ts
git commit -m "types(web-admin): Order/Product 加新字段 + 2 个 admin caller

Order 加 notes/tracking/userSnapshot 新形态。
Product 加 7 个扩展字段。
api/order.ts 加 updateTracking + updateOrderNotes callers。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task D2: web-admin OrderDetail 加 3 个 Card

**Files:**
- Modify: `web-admin/src/pages/OrderDetail.tsx`

- [ ] **Step 1: 加 3 个 Card**

读现有 OrderDetail.tsx,在 "运费尾款" Card 之后加(antd 风格,follow现有 Space + Card pattern):

```tsx
import { updateTracking, updateOrderNotes } from '../api/order';

const COURIER_OPTIONS = ['顺丰', '中通', '圆通', '极兔', 'EMS', 'Australia Post', 'StarTrack', '其他'];

// 在主 return 内,运费 Card 之后:

<Card title="物流跟踪" size="small">
  {order.tracking?.courierNo ? (
    <Space direction="vertical" size={8} style={{ display: 'flex' }}>
      <div>重量:{order.tracking.weight} kg</div>
      <div>快递:{order.tracking.courierName}</div>
      <div>单号:{order.tracking.courierNo}</div>
      <Form
        layout="inline"
        initialValues={{
          weight: order.tracking.weight,
          courierName: order.tracking.courierName,
          courierNo: order.tracking.courierNo,
        }}
        onFinish={async (vals: { weight: number; courierName: string; courierNo: string }) => {
          try {
            await updateTracking(order._id, vals);
            message.success('物流已更新');
            await load();
          } catch (e: any) { message.error(e.message); }
        }}
      >
        <Form.Item name="weight"><InputNumber min={0} step={0.1} addonAfter="kg" /></Form.Item>
        <Form.Item name="courierName">
          <Select options={COURIER_OPTIONS.map((c) => ({ label: c, value: c }))} style={{ width: 120 }} />
        </Form.Item>
        <Form.Item name="courierNo"><Input placeholder="单号" /></Form.Item>
        <Form.Item><Button htmlType="submit">改物流</Button></Form.Item>
      </Form>
    </Space>
  ) : (
    <Form
      layout="inline"
      onFinish={async (vals: { weight: number; courierName: string; courierNo: string }) => {
        try {
          await updateTracking(order._id, vals);
          message.success('物流已设置');
          await load();
        } catch (e: any) { message.error(e.message); }
      }}
    >
      <Form.Item name="weight" rules={[{ required: true, message: '请输入重量' }]}>
        <InputNumber min={0} step={0.1} addonAfter="kg" />
      </Form.Item>
      <Form.Item name="courierName" rules={[{ required: true, message: '请选快递' }]}>
        <Select options={COURIER_OPTIONS.map((c) => ({ label: c, value: c }))} placeholder="快递公司" style={{ width: 140 }} />
      </Form.Item>
      <Form.Item name="courierNo" rules={[{ required: true, message: '请填单号' }]}>
        <Input placeholder="快递单号" />
      </Form.Item>
      <Form.Item>
        <Button type="primary" htmlType="submit">设置物流</Button>
      </Form.Item>
    </Form>
  )}
</Card>

<Card title="卖家备注 (买家看得到)" size="small">
  <Form
    layout="vertical"
    initialValues={{ sellerNote: order.notes?.seller || '' }}
    onFinish={async (vals: { sellerNote: string }) => {
      try {
        await updateOrderNotes(order._id, vals.sellerNote);
        message.success('备注已保存');
        await load();
      } catch (e: any) { message.error(e.message); }
    }}
  >
    <Form.Item name="sellerNote">
      <Input.TextArea rows={3} maxLength={500} showCount placeholder="留言给买家,如:明天发货" />
    </Form.Item>
    <Form.Item>
      <Button htmlType="submit">保存备注</Button>
    </Form.Item>
  </Form>
</Card>
```

- [ ] **Step 2: 顶部 Descriptions 加客户群号 + 替换昵称展示**

找到现有 Descriptions 块(展示客户姓名/电话等):

```tsx
<Descriptions.Item label="客户姓名">{order.userSnapshot.name}</Descriptions.Item>
<Descriptions.Item label="客户电话">{order.userSnapshot.phone}</Descriptions.Item>
```

替换为:
```tsx
<Descriptions.Item label="客户昵称">
  {order.userSnapshot.nickname || order.userSnapshot.name || '—'}
</Descriptions.Item>
<Descriptions.Item label="客户群号">
  {order.userSnapshot.groupId || '—'}
</Descriptions.Item>
{order.userSnapshot.phone && (
  <Descriptions.Item label="客户电话(legacy)">{order.userSnapshot.phone}</Descriptions.Item>
)}
```

- [ ] **Step 3: 需要的 import 加全**

```tsx
import { Input, Select, InputNumber, Form, Button, Card, Space, message } from 'antd';
```

(检查是否已经 import,缺什么补什么。Antd 5 风格)

- [ ] **Step 4: tsc check**

```bash
cd web-admin && npx tsc --noEmit 2>&1 | head
```

- [ ] **Step 5: Commit**

```bash
git add web-admin/src/pages/OrderDetail.tsx
git commit -m "feat(web-admin/OrderDetail): 物流跟踪 + 卖家备注 Card + 客户群号显示

3 个变更:
- 物流跟踪 Card:重量/快递公司/单号,3 状态(没设/已设/编辑)
- 卖家备注 Card:Textarea 500 字,提示买家看得到
- 顶部 Descriptions:客户姓名 → 客户昵称(wechat),加客户群号一行

调 _admin/updateTracking + _admin/updateOrderNotes。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task D3: web-admin ProductEdit 加 7 字段

**Files:**
- Modify: `web-admin/src/pages/ProductEdit.tsx`

- [ ] **Step 1: 读现有 ProductEdit.tsx**

```bash
cat web-admin/src/pages/ProductEdit.tsx | head -80
```

- [ ] **Step 2: 加 7 个 Form.Item**

在现有 title/desc/coverFileId 等 Form.Item 之后追加新分组:

```tsx
import { Card, Input, InputNumber, Select, Form, Button, Space, Upload } from 'antd';

const COURIER_OPTIONS = ['顺丰', '中通', '圆通', '极兔', 'EMS', 'Australia Post', 'StarTrack', '其他'];

// 在 Form 内,放在现有字段之后:

<Card title="商品资料(扩展)" size="small" style={{ marginTop: 16 }}>
  <Form.Item name="brand" label="品牌">
    <Input maxLength={50} placeholder="如:Coles / Woolworths" />
  </Form.Item>
  <Form.Item name="spec" label="规格">
    <Input maxLength={100} placeholder="如:500g/盒 或 1.2L 装" />
  </Form.Item>
  <Form.Item name="basePrice" label="正常收录价(¥)" tooltip="reference 用,不参与团购定价">
    <InputNumber min={0} step={0.01} style={{ width: '100%' }} placeholder="正常零售价" />
  </Form.Item>
  <Form.Item name="englishName" label="英文名">
    <Input maxLength={200} placeholder="Australian Beef 500g" />
  </Form.Item>
  <Form.Item name="courierName" label="快递公司">
    <Select options={COURIER_OPTIONS.map((c) => ({ label: c, value: c }))} placeholder="选快递" allowClear />
  </Form.Item>
  <Form.Item name="courierFactor" label="快递系数" tooltip="按重量算运费的乘数,0~10">
    <InputNumber min={0} max={10} step={0.1} style={{ width: '100%' }} />
  </Form.Item>
  <Form.List name="secondaryImages">
    {(fields, { add, remove }) => (
      <>
        <div style={{ marginBottom: 8 }}>次要图片</div>
        {fields.map((field) => (
          <Space key={field.key} style={{ display: 'flex', marginBottom: 8 }} align="baseline">
            <Form.Item {...field} name={[field.name, 'url']} rules={[{ required: true }]}>
              <Input placeholder="图片 URL" style={{ width: 250 }} />
            </Form.Item>
            <Form.Item {...field} name={[field.name, 'caption']}>
              <Input placeholder="说明" style={{ width: 200 }} />
            </Form.Item>
            <Button onClick={() => remove(field.name)}>删</Button>
          </Space>
        ))}
        <Button onClick={() => add({ url: '', caption: '' })}>+ 加次要图片</Button>
      </>
    )}
  </Form.List>
</Card>
```

submit 提交时(现有 onFinish)把这些字段一起提交,后端 productCRUD 已经接(Task A6 实现)。

basePrice 提交时元 → 分:在 onFinish 转换:
```ts
data: {
  ...vals,
  basePrice: vals.basePrice != null ? Math.round(vals.basePrice * 100) : undefined,
}
```

读时 initialValues 分 → 元:
```ts
initialValues={{
  ...product,
  basePrice: product?.basePrice != null ? product.basePrice / 100 : undefined,
}}
```

- [ ] **Step 3: tsc check**

```bash
cd web-admin && npx tsc --noEmit 2>&1 | head
```

- [ ] **Step 4: Commit**

```bash
git add web-admin/src/pages/ProductEdit.tsx
git commit -m "feat(web-admin/ProductEdit): 加 7 个商品扩展字段

商品资料新分组 Card,7 字段:
- brand / spec / basePrice (¥, 内部 cents) / englishName
- courierName (Select enum) / courierFactor (0-10)
- secondaryImages (动态 Form.List,每行 URL + caption)

basePrice 显示 ¥ 提交 cents,UI 友好。

后端 productCRUD 已接字段(Task A6)。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task D4: web-admin TuanEdit product 联动

**Files:**
- Modify: `web-admin/src/pages/TuanEdit.tsx`

- [ ] **Step 1: 读现有 TuanEdit**

```bash
cat web-admin/src/pages/TuanEdit.tsx | head -80
```

定位 tuan_item 创建/编辑表单的 productId Select(或 Cascader)位置。

- [ ] **Step 2: 加 onChange 联动**

在 productId Select 上挂 onChange,从已加载的 products 列表里找选中的 product,自动 fill title/price/spec 等到 form:

```tsx
// 假设 products 是已加载的商品列表 state(现有代码)
// 假设 form 是 antd useForm 实例(现有)

<Form.Item name="productId" label="商品" rules={[{ required: true }]}>
  <Select
    showSearch
    options={products.map(p => ({ label: p.title, value: p._id }))}
    onChange={(productId) => {
      const product = products.find(p => p._id === productId);
      if (!product) return;
      // 自动 fill,但只填空字段(用户已改的不覆盖)
      const current = form.getFieldsValue();
      const patch: any = { productId };
      if (!current.title) patch.title = product.title;
      if (current.price == null && product.basePrice != null) {
        patch.price = product.basePrice / 100; // 显示 ¥
      }
      form.setFieldsValue(patch);
    }}
  />
</Form.Item>

{/* 在 productId 下方加 readonly 展示从 product 来的元数据 */}
<Form.Item shouldUpdate>
  {() => {
    const productId = form.getFieldValue('productId');
    const p = products.find(x => x._id === productId);
    if (!p) return null;
    return (
      <div style={{ background: '#f5f5f5', padding: 8, marginBottom: 16 }}>
        <div style={{ fontSize: 12, color: '#666' }}>
          {p.brand && <span>品牌:{p.brand} · </span>}
          {p.spec && <span>规格:{p.spec} · </span>}
          {p.englishName && <span>英文名:{p.englishName}</span>}
        </div>
      </div>
    );
  }}
</Form.Item>
```

- [ ] **Step 3: tsc check**

```bash
cd web-admin && npx tsc --noEmit 2>&1 | head
```

- [ ] **Step 4: Commit**

```bash
git add web-admin/src/pages/TuanEdit.tsx
git commit -m "feat(web-admin/TuanEdit): 选 product 时自动 fill 默认值

product 选中后:
- title 空时 → fill from product.title
- price 空时 → fill from product.basePrice (cents→元)
- 已填字段不覆盖(尊重用户改动)
- readonly 展示 product 元数据(品牌/规格/英文名)在下方淡灰条

不破坏现有 copyFromTuan 整团复制流程。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase E — 配置清理 + 部署 + 验证(3 tasks)

### Task E1: docker-compose 删 SMS / BUMP env

**Files:**
- Modify: `deploy/docker-compose.production.yml`

- [ ] **Step 1: 删除 env 行**

读 `deploy/docker-compose.production.yml` 找 api service 的 environment block,删除:

```yaml
      BUMP_EXPIRED_TUANS: "0"
      SMS_STUB: ${SMS_STUB:-1}
      TENCENT_SMS_SECRET_ID: ${TENCENT_SMS_SECRET_ID:-}
      TENCENT_SMS_SECRET_KEY: ${TENCENT_SMS_SECRET_KEY:-}
      TENCENT_SMS_APP_ID: ${TENCENT_SMS_APP_ID:-}
      TENCENT_SMS_SIGN: ${TENCENT_SMS_SIGN:-}
      TENCENT_SMS_TEMPLATE_OTP: ${TENCENT_SMS_TEMPLATE_OTP:-}
      TENCENT_SMS_REGION: ${TENCENT_SMS_REGION:-ap-guangzhou}
```

- [ ] **Step 2: 验证 compose 文件语法**

```bash
docker compose -f deploy/docker-compose.production.yml config 2>&1 | head -5
```

(若本地无 docker compose 命令,跳过这步,直接 commit)

- [ ] **Step 3: Commit**

```bash
git add deploy/docker-compose.production.yml
git commit -m "chore(deploy): 删 SMS_* + BUMP_EXPIRED_TUANS env

OAuth-only 之后 SMS 全部用不上。BUMP_EXPIRED_TUANS 是 staging 调试
工具,生产不用。

如果 deploy/.env.staging 里还有这些 key,运行时只是 unused,不影响。
后续可在 deploy/.env.* 里 cleanup。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task E2: 一次性 mongo drop otp_codes

> 这步在 staging 上 deploy 完成后跑,不需要 commit。

- [ ] **Step 1: deploy 跑完后执行**

```bash
gh run watch $(gh run list --workflow="Deploy App" --limit 1 --json databaseId -q '.[0].databaseId') --exit-status

# deploy 成功后:
ssh -i ~/.ssh/mogu_deploy ubuntu@43.159.198.145 \
  'sudo docker exec mogu_mongo mongosh --quiet --eval "
    db.getSiblingDB(\"mogu_express\").otp_codes.drop()
  "'
```

Expected: `true`(成功 drop)或 `false`(collection 不存在,无所谓)

- [ ] **Step 2: 确认 OTP collection 没了**

```bash
ssh -i ~/.ssh/mogu_deploy ubuntu@43.159.198.145 \
  'sudo docker exec mogu_mongo mongosh --quiet --eval "
    db.getSiblingDB(\"mogu_express\").getCollectionNames()
  "' | tr ',' '\n' | grep -i otp
```

Expected: 无输出(otp_codes 已删)

---

### Task E3: E2E staging 验证

> 手测,无 commit。

- [ ] **微信内全链路**:
  1. 微信里点 https://shop-staging.moguexpress.com → OAuth 静默
  2. 新用户跳 /register-profile → 看到群号 input(无 name/phone field)
  3. 填群号"测试群" → 提交 → 跳首页
  4. 加购 → 结算 → 提交订单(应该 0 报错,**没有"请先完善姓名电话"了**)
  5. 我的订单 → 看到刚下的订单(不强求 banner)

- [ ] **mongo 查 user 文档**:
  ```bash
  ssh ubuntu@$VPS 'sudo docker exec mogu_mongo mongosh --quiet --eval "
    db.getSiblingDB(\"mogu_express\").users.findOne({groupId:{\$exists:true}}, {_openid:1, groupId:1, wechat:1})
  "'
  ```
  期望:`{_openid:'o...', groupId:'测试群', wechat:{nickname:...}}`

- [ ] **mongo 查 order userSnapshot**:
  ```bash
  ssh ubuntu@$VPS 'sudo docker exec mogu_mongo mongosh --quiet --eval "
    db.getSiblingDB(\"mogu_express\").orders.findOne({},{orderNo:1, userSnapshot:1, notes:1}, {sort:{createdAt:-1}})
  "'
  ```
  期望:userSnapshot 含 nickname/avatar/groupId,无 name/phone。notes.buyer 来自 remark 入参。

- [ ] **Admin 设物流 + 卖家备注**:
  1. Admin 进刚下的订单详情
  2. 顶部 Descriptions 看到客户昵称 + 客户群号("测试群")
  3. "物流跟踪" Card 输 重量=1 / 快递=顺丰 / 单号=SF123 → 设置 → 成功
  4. "卖家备注" Card 输"明早发货" → 保存 → 成功

- [ ] **用户端订单详情验**:
  1. 微信刷新订单详情
  2. 看到"物流信息" Card:重量 1 kg / 快递 顺丰 / 单号 SF123 + 复制按钮
  3. 看到"📣 团长留言" Card:"明早发货"(淡黄背景)

- [ ] **Admin Product Edit**:
  1. 进任一商品编辑
  2. 看到"商品资料(扩展)" Card
  3. 填 brand=Coles / spec=500g / basePrice=¥45.00 / 英文名 / 快递=顺丰 / 系数=1.5 → 保存
  4. 刷新 → 字段全部保留

- [ ] **Admin Tuan Edit 联动**:
  1. 创建新 tuan_item,选 product
  2. title / price 自动 fill(若空)
  3. 下方灰条显示 product 元数据(brand/spec/englishName)

- [ ] **非微信入口**:
  1. 普通浏览器(Chrome)打开 https://shop-staging.moguexpress.com
  2. 跳 Protected 路径(如 /orders)→ 应跳 /qr-fallback(显示二维码)
  3. ✗ 不应跳 /login(已删)

---

## Self-Review

### Spec coverage 对照

| Spec section | Plan task(s) |
|---|---|
| 1. data schema(users.groupId / orders.tracking/notes/userSnapshot / products 7 字段) | A1 / A2 / A4 / A5 / A6 |
| 2. admin UI(OrderDetail / ProductEdit / TuanEdit) | D2 / D3 / D4 |
| 3. 用户端 UI(RegisterProfile / OrderDetail / App.tsx Protected) | C3 / C4 / C5 |
| 4. 后端 cf 改动 + 删 OTP | A1-A6 / B1 |
| 5. 测试 + 部署 + Rollback | A1-A6 测试嵌入,E1-E3 部署 |
| store/auth.ts UserProfile.groupId | C1 |
| types/index.ts Order/userSnapshot 重定义 | C1 |
| web-admin types.ts | D1 |
| web-admin api callers | D1 |

无 gap。

### Placeholder scan

- A6 "follow现有 section 字段的处理风格" — 引导但不指明具体 line。这是合理的因为现有代码我没读完。实施者读 productCRUD 即可。✓
- D2 "follow现有 antd Card pattern" — 同理,实施者读 OrderDetail.tsx 即可。✓
- D3 "follow现有 Form.List pattern" — antd 标准 API。✓
- 其他步骤均有完整代码块或精确路径。

### Type consistency

- `userSnapshot` shape:Task A2 写 `{nickname, avatar, groupId}`,Task C1 / D1 types 也是 `{nickname?, avatar?, groupId?, name?, phone?}`(后两个 legacy 可选)。一致 ✓
- `tracking` shape:Task A4 写 `{weight, courierName, courierNo, setAt}`,types(C1/D1)一致。✓
- `notes` shape:Task A2/A5 写 `{buyer, seller}`,types 一致。✓
- `WxLoginRes.user.groupId` 加在 C2,store/auth.ts UserProfile.groupId 加在 C1。一致 ✓
- `Product` 7 字段:Task A6 后端、C1 没动(web-shop 不需要扩展 product 字段)、D1 web-admin 加上。一致 ✓

### Type definition order

C1(types)在 C2/C3/C5(用 types)之前 ✓
D1(types + api)在 D2(用 api)之前 ✓
A2(改 createOrder)之前先 A1(改 registerProfile)。这俩独立 ✓

### 任务总数

15 task(A1-A6 / B1 / C1-C5 / D1-D4 / E1-E3)。
预计 6-8h focused work。

---

## 估算

| Phase | Tasks | 预计 |
|---|---|---|
| A 后端云函数 | A1-A6 | ~3 h |
| B 删 OTP cf | B1 | 5 min |
| C 前端 web-shop | C1-C5 | ~2 h |
| D 前端 web-admin | D1-D4 | ~2.5 h |
| E 配置 + 部署 + 验证 | E1-E3 | ~30 min |
| **合计** | **15 tasks** | **~8 hours focused** |

预计 1.5-2 天(含 review iterations、跨 task context 切换、subagent 调度开销)。
