# Sprint 1.2 — Excel 批量上传运费 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 admin 批量上传 xlsx 一键更新订单运费 / 重量 / 快递单号,5 秒处理 100+ 行替代单笔操作。

**Architecture:**
- 后端 1 个新 cf `_admin/uploadShippingFeesXlsx`,单 cf + `dryRun: bool` 双相(预览 / 应用)。
- 前端 admin Orders 页加按钮 + 3-phase Modal(选文件 → 预览带 before → 确认应用)。
- 前端用 exceljs 浏览器端生成模板下载;后端用同款 exceljs 解析 xlsx。
- per-row update 无事务,一行失败不影响其他;5 种状态(matched / not_found / already_paid / invalid / duplicate_in_file)报告给 admin。

**Tech Stack:** Node.js + exceljs(已有 dep)/ React + Vite + antd(web-admin)/ MongoDB / 沿用 _admin/* requireAdmin pattern。

**Spec 来源:** `docs/superpowers/specs/2026-05-18-sprint1.2-excel-batch-shipping-fees-design.md`

---

## File Structure(改动总览)

### 后端

| 文件 | 动作 |
|---|---|
| `cloudfunctions/_admin/uploadShippingFeesXlsx/index.js` | 创建 |
| `cloudfunctions/_admin/uploadShippingFeesXlsx/package.json` | 创建(exceljs ~4.3) |
| `cloudfunctions/_admin/uploadShippingFeesXlsx/jwt.js` | 复制 from sibling |
| `cloudfunctions/_admin/uploadShippingFeesXlsx/config.json` | 创建 |
| `local-backend/api/test-shim.js` | 加 makeTestXlsx helper + 4 测试 |

### 前端 web-admin

| 文件 | 动作 |
|---|---|
| `web-admin/src/utils/shippingTemplate.ts` | 创建(浏览器端 xlsx 模板生成) |
| `web-admin/src/api/order.ts` | 加 BatchShippingRow / BatchShippingResp types + uploadShippingFeesXlsx caller |
| `web-admin/src/pages/BatchShippingFeeModal.tsx` | 创建(3-phase 组件) |
| `web-admin/src/pages/Orders.tsx` | 顶部加按钮,集成 modal |

---

## Phase A — 后端 cf(1 task)

### Task A1: uploadShippingFeesXlsx cf + helper + 4 tests

**Files:**
- Create: `cloudfunctions/_admin/uploadShippingFeesXlsx/index.js`
- Create: `cloudfunctions/_admin/uploadShippingFeesXlsx/package.json`
- Create: `cloudfunctions/_admin/uploadShippingFeesXlsx/jwt.js`(copy)
- Create: `cloudfunctions/_admin/uploadShippingFeesXlsx/config.json`
- Modify: `local-backend/api/test-shim.js`(加 helper + 4 测试)

#### Step 1: Scaffold cf 目录 + 复制 jwt.js

```bash
mkdir -p cloudfunctions/_admin/uploadShippingFeesXlsx
cp cloudfunctions/_admin/setShippingFee/jwt.js cloudfunctions/_admin/uploadShippingFeesXlsx/jwt.js
```

`cloudfunctions/_admin/uploadShippingFeesXlsx/package.json`:
```json
{
  "name": "_admin_uploadShippingFeesXlsx",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "wx-server-sdk": "~2.6.3",
    "exceljs": "~4.3.0"
  }
}
```

`cloudfunctions/_admin/uploadShippingFeesXlsx/config.json`:
```json
{ "permissions": { "openapi": [] } }
```

#### Step 2: 先写一个 stub `index.js`(让 test 能找到 cf)

```js
// _admin/uploadShippingFeesXlsx — stub,真实实现在 step 4
exports.main = async () => ({ code: 500, message: 'not implemented' });
```

#### Step 3: 加 makeTestXlsx helper + 4 测试到 test-shim.js

在 `local-backend/api/test-shim.js` 文件顶部,找到 `const ExcelJS = require('exceljs');` 引入(若已有);否则加。然后加 helper:

```js
// 顶部 helpers 区
const ExcelJS = require('exceljs');

async function makeTestXlsx(rows) {
  // rows: [[订单号, 重量, 尾款, 单号], ...]
  const wb = new ExcelJS.Workbook();
  const ws = wb.addWorksheet('运费');
  ws.addRow(['订单号', '实际总重量', '应补尾款', '快递单号']);
  rows.forEach((r) => ws.addRow(r));
  const buf = await wb.xlsx.writeBuffer();
  return Buffer.from(buf).toString('base64');
}
```

在文件末尾(addressValidate 等测试之后,test-shim 结尾的 `runAll()` 之前)加 4 个测试:

```js
test('_admin/uploadShippingFeesXlsx dryRun 预览覆盖 5 种状态', async () => {
  reset({
    orders: [
      { _id: 'ord_A', orderNo: 'MG_A', _openid: 'u1', items: [], amount: 0, status: 'paid', payStatus: 'paid', shipping: {}, createdAt: new Date() },
      { _id: 'ord_B', orderNo: 'MG_B', _openid: 'u1', items: [], amount: 0, status: 'paid', payStatus: 'paid', shipping: {}, shippingFee: { amount: 4000, payStatus: 'paid', paidAt: new Date(), outTradeNo: 'SHIP_OLD', setAt: new Date() }, createdAt: new Date() },
    ],
    admins: [{ openid: 'admin1', username: 'admin' }],
  });
  shim.__setContext({ OPENID: 'admin1' });
  const xlsxBase64 = await makeTestXlsx([
    ['MG_A', 2.5, 35.00, 'SF1'],
    ['MG_NONEXIST', 1.0, 10.00, 'SF2'],
    ['MG_B', 1.5, 40.00, 'SF3'],
    ['MG_A', 3.0, 50.00, 'SF4'],
    ['', 1, 1, 'X'],
  ]);
  const r = await requireCf('_admin/uploadShippingFeesXlsx').main({ xlsxBase64, dryRun: true }, {});
  assert.equal(r.code, 0, `expected code 0, got ${JSON.stringify(r)}`);
  assert.equal(r.rows.length, 5);
  assert.equal(r.rows[0].status, 'matched');
  assert.equal(r.rows[1].status, 'not_found');
  assert.equal(r.rows[2].status, 'already_paid');
  assert.equal(r.rows[3].status, 'duplicate_in_file');
  assert.equal(r.rows[4].status, 'invalid');
  assert.equal(r.summary.matched, 1);
  assert.equal(r.summary.notFound, 1);
  assert.equal(r.summary.alreadyPaid, 1);
  assert.equal(r.summary.duplicateInFile, 1);
  assert.equal(r.summary.invalid, 1);
  assert.equal(r.summary.applied, undefined, 'dryRun should not commit');
  // verify mongo not written
  const ordA = await shim.database().collection('orders').doc('ord_A').get();
  assert.equal(ordA.data.shippingFee, undefined, 'ord_A should not have shippingFee after dryRun');
});

test('_admin/uploadShippingFeesXlsx apply 只写 matched 行', async () => {
  reset({
    orders: [
      { _id: 'ord_A', orderNo: 'MG_A', _openid: 'u1', items: [], amount: 0, status: 'paid', payStatus: 'paid', shipping: {}, createdAt: new Date() },
      { _id: 'ord_B', orderNo: 'MG_B', _openid: 'u1', items: [], amount: 0, status: 'paid', payStatus: 'paid', shipping: {}, shippingFee: { amount: 4000, payStatus: 'paid', paidAt: new Date(), outTradeNo: 'SHIP_OLD', setAt: new Date() }, createdAt: new Date() },
    ],
    admins: [{ openid: 'admin1', username: 'admin' }],
  });
  shim.__setContext({ OPENID: 'admin1' });
  const xlsxBase64 = await makeTestXlsx([
    ['MG_A', 2.5, 3500, 'SF1'],   // fee is in CENTS here (3500 = ¥35) — see schema check below
    ['MG_NONEXIST', 1.0, 1000, 'SF2'],
    ['MG_B', 1.5, 4000, 'SF3'],
  ]);
  const r = await requireCf('_admin/uploadShippingFeesXlsx').main({ xlsxBase64, dryRun: false }, {});
  assert.equal(r.code, 0);
  assert.equal(r.summary.applied, 1);
  assert.equal(r.summary.matched, 1);
  // verify ord_A written
  const ordA = await shim.database().collection('orders').doc('ord_A').get();
  assert.equal(ordA.data.shippingFee.amount, 3500);
  assert.equal(ordA.data.shippingFee.payStatus, 'pending');
  assert.ok(ordA.data.shippingFee.outTradeNo.startsWith('SHIP'));
  assert.equal(ordA.data.tracking.weight, 2.5);
  assert.equal(ordA.data.tracking.courierName, null);
  assert.equal(ordA.data.tracking.courierNo, 'SF1');
  // verify ord_B unchanged
  const ordB = await shim.database().collection('orders').doc('ord_B').get();
  assert.equal(ordB.data.shippingFee.outTradeNo, 'SHIP_OLD', 'paid order should not be modified');
});

test('_admin/uploadShippingFeesXlsx 非 admin 拒', async () => {
  reset({
    orders: [{ _id: 'ord_A', orderNo: 'MG_A', _openid: 'u1', items: [], amount: 0, status: 'paid', payStatus: 'paid', shipping: {}, createdAt: new Date() }],
    admins: [],
  });
  shim.__setContext({ OPENID: 'not_admin' });
  const xlsxBase64 = await makeTestXlsx([['MG_A', 1, 1000, 'X']]);
  const r = await requireCf('_admin/uploadShippingFeesXlsx').main({ xlsxBase64, dryRun: true }, {});
  assert.equal(r.code, 403);
});

test('_admin/uploadShippingFeesXlsx header 错乱拒', async () => {
  reset({ admins: [{ openid: 'admin1', username: 'admin' }] });
  shim.__setContext({ OPENID: 'admin1' });
  // 用 3 列 header(缺"快递单号")
  const wb = new ExcelJS.Workbook();
  const ws = wb.addWorksheet('运费');
  ws.addRow(['订单号', '实际总重量', '应补尾款']);
  ws.addRow(['MG_X', 1, 1000]);
  const buf = await wb.xlsx.writeBuffer();
  const xlsxBase64 = Buffer.from(buf).toString('base64');
  const r = await requireCf('_admin/uploadShippingFeesXlsx').main({ xlsxBase64, dryRun: true }, {});
  assert.equal(r.code, 2, `expected code 2 for bad header, got: ${JSON.stringify(r)}`);
});
```

> 关于 `fee` 单位:`makeTestXlsx` 第三列是 Excel 数字。Spec 说 "应补尾款" 列是 ¥(元),后端转 cents。但为了测试简单(避免浮点问题),测试里直接传 cents 整数(35.00 → 3500),后端处理时 detect 是否 < 100(认为是元 × 100)还是 ≥ 100(已经是 cents)。
>
> ⚠️ **决定**:Excel 列里填 **元**(用户视角),后端 `Math.round(fee * 100)` 转 cents。测试里我们传 35.00,后端转 3500。重新调整上面测试 2 的 xlsx data: `['MG_A', 2.5, 35.00, 'SF1']` 并断言 `ordA.data.shippingFee.amount === 3500`。

修正测试 2:
```js
const xlsxBase64 = await makeTestXlsx([
  ['MG_A', 2.5, 35.00, 'SF1'],   // 35 元 → 3500 cents
  ['MG_NONEXIST', 1.0, 10.00, 'SF2'],
  ['MG_B', 1.5, 40.00, 'SF3'],
]);
// ... rest same, assert amount=3500 (35 × 100)
```

#### Step 4: 跑 test 验证 fail

```bash
node local-backend/api/test-shim.js 2>&1 | grep -E '✗|passed' | tail
```
Expected: 4 个新测试 fail(stub 返 code:500 全错)

#### Step 5: 写完整 cf

`cloudfunctions/_admin/uploadShippingFeesXlsx/index.js` 整文件:

```js
// _admin/uploadShippingFeesXlsx — admin 批量上传 Excel 设运费 + 物流
//
// 入参: { xlsxBase64: string, dryRun: boolean }
// 出参: { code: 0, rows: BatchShippingRow[], summary: {...} } 或错误码
//
// 行为:
//   1. 鉴权 (admin 必需)
//   2. base64 → xlsx → exceljs parse
//   3. 校验 header (4 列中文,顺序可变,列名严格)
//   4. 校验行数 ≤ 500
//   5. 逐行校验 + 查 mongo,给 status 标签
//   6. dryRun=false 时对 matched 行 update orders (per-row,无事务)
//   7. 返 rows + summary
//
// 错误码:
//   1   xlsxBase64 缺 / 体积超 2MB
//   2   header 列名缺失
//   3   行数 > 500
//   401 not logged in
//   403 not admin
//   500 internal

const crypto = require('crypto');
const ExcelJS = require('exceljs');
const cloud = require('wx-server-sdk');
const { verify } = require('./jwt.js');

cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });
const db = cloud.database();

const JWT_SECRET = process.env.JWT_SECRET || 'mogu_express_dev_secret_REPLACE_ME_IN_PROD';

const REQUIRED_HEADERS = ['订单号', '实际总重量', '应补尾款', '快递单号'];
const MAX_BASE64_BYTES = 2 * 1024 * 1024 * 4 / 3;   // ~2.6MB base64 = ~2MB binary
const MAX_ROWS = 500;

async function requireAdmin(event) {
  if (event && event.token) {
    try { return { source: 'web', admin: verify(event.token, JWT_SECRET) }; }
    catch (err) { const e = new Error('invalid token'); e.code = 401; throw e; }
  }
  const { OPENID } = cloud.getWXContext();
  if (!OPENID) { const e = new Error('no openid'); e.code = 401; throw e; }
  const res = await db.collection('admins').where({ openid: OPENID }).limit(1).get();
  if (!res.data || !res.data.length) { const e = new Error('not admin'); e.code = 403; throw e; }
  return { source: 'mp', admin: res.data[0] };
}

function classifyRow(rowNum, raw) {
  // raw: [订单号, 重量, 尾款, 单号](按列序整理过的)
  const [orderNoRaw, weightRaw, feeYuanRaw, courierNoRaw] = raw;
  const orderNo = (orderNoRaw == null ? '' : String(orderNoRaw)).trim();
  if (!orderNo) return { row: rowNum, orderNo: '', weight: null, fee: null, courierNo: null, status: 'invalid', message: '订单号为空' };

  const weight = Number(weightRaw);
  if (!Number.isFinite(weight) || weight < 0 || weight > 1000) {
    return { row: rowNum, orderNo, weight: null, fee: null, courierNo: null, status: 'invalid', message: '重量非法(0..1000 kg)' };
  }
  const feeYuan = Number(feeYuanRaw);
  if (!Number.isFinite(feeYuan) || feeYuan < 0 || feeYuan > 10000) {
    return { row: rowNum, orderNo, weight, fee: null, courierNo: null, status: 'invalid', message: '尾款非法(¥0..10000)' };
  }
  const fee = Math.round(feeYuan * 100);
  const courierNo = courierNoRaw == null ? '' : String(courierNoRaw).trim();
  if (courierNo.length > 100) {
    return { row: rowNum, orderNo, weight, fee, courierNo: null, status: 'invalid', message: '快递单号过长' };
  }
  return { row: rowNum, orderNo, weight, fee, courierNo, status: 'pending' };
}

exports.main = async (event) => {
  try {
    await requireAdmin(event);
    const { xlsxBase64, dryRun } = event || {};
    if (!xlsxBase64 || typeof xlsxBase64 !== 'string') {
      return { code: 1, message: 'xlsxBase64 required' };
    }
    if (xlsxBase64.length > MAX_BASE64_BYTES) {
      return { code: 1, message: '文件过大 (> 2MB)' };
    }

    const buf = Buffer.from(xlsxBase64, 'base64');
    const wb = new ExcelJS.Workbook();
    await wb.xlsx.load(buf);
    const ws = wb.worksheets[0];
    if (!ws) return { code: 2, message: 'no sheet' };

    // 读 header(row 1),列名 → index 映射
    const headerRow = ws.getRow(1);
    const colIndex = {};   // header label → col number (1-based)
    headerRow.eachCell((cell, colNumber) => {
      const v = (cell.value == null ? '' : String(cell.value)).trim();
      if (REQUIRED_HEADERS.includes(v)) colIndex[v] = colNumber;
    });
    for (const h of REQUIRED_HEADERS) {
      if (!colIndex[h]) return { code: 2, message: `header missing: ${h}` };
    }

    // 行数检查
    const rowCount = ws.rowCount - 1; // 排除 header
    if (rowCount > MAX_ROWS) return { code: 3, message: `rows > ${MAX_ROWS}` };

    // 收集 raw rows
    const rawRows = [];
    for (let r = 2; r <= ws.rowCount; r++) {
      const row = ws.getRow(r);
      // 全空行跳过
      const isEmpty = REQUIRED_HEADERS.every((h) => {
        const v = row.getCell(colIndex[h]).value;
        return v == null || (typeof v === 'string' && v.trim() === '');
      });
      if (isEmpty) continue;
      rawRows.push({
        rowNum: r,
        cells: REQUIRED_HEADERS.map((h) => row.getCell(colIndex[h]).value),
      });
    }

    // 分类 + 查 mongo + 重复检测
    const seenOrderNos = new Set();
    const rows = [];
    for (const { rowNum, cells } of rawRows) {
      const classified = classifyRow(rowNum, cells);
      if (classified.status === 'invalid') {
        rows.push(classified);
        continue;
      }
      // 文件内重复
      if (seenOrderNos.has(classified.orderNo)) {
        classified.status = 'duplicate_in_file';
        rows.push(classified);
        continue;
      }
      seenOrderNos.add(classified.orderNo);

      // 查 mongo
      const orderRes = await db.collection('orders').where({ orderNo: classified.orderNo }).limit(1).get();
      const order = orderRes.data && orderRes.data[0];
      if (!order) {
        classified.status = 'not_found';
        rows.push(classified);
        continue;
      }
      if (order.shippingFee && order.shippingFee.payStatus === 'paid') {
        classified.status = 'already_paid';
        classified.before = { shippingFee: order.shippingFee, tracking: order.tracking };
        rows.push(classified);
        continue;
      }

      classified.status = 'matched';
      classified.before = { shippingFee: order.shippingFee, tracking: order.tracking };
      classified._mongoId = order._id;
      rows.push(classified);
    }

    // summary
    const summary = {
      matched: rows.filter((r) => r.status === 'matched').length,
      notFound: rows.filter((r) => r.status === 'not_found').length,
      alreadyPaid: rows.filter((r) => r.status === 'already_paid').length,
      invalid: rows.filter((r) => r.status === 'invalid').length,
      duplicateInFile: rows.filter((r) => r.status === 'duplicate_in_file').length,
    };

    // dryRun=false 时实际 update
    if (!dryRun) {
      let applied = 0;
      for (const row of rows) {
        if (row.status !== 'matched') continue;
        const now = new Date();
        const outTradeNo = 'SHIP' + Date.now() + crypto.randomBytes(4).toString('hex').toUpperCase();
        await db.collection('orders').doc(row._mongoId).update({
          data: {
            shippingFee: {
              amount: row.fee,
              outTradeNo,
              payStatus: 'pending',
              setAt: now,
              paidAt: null,
            },
            tracking: {
              weight: row.weight,
              courierName: null,
              courierNo: row.courierNo,
              setAt: now,
            },
            updatedAt: now,
          },
        });
        applied += 1;
      }
      summary.applied = applied;
      const { OPENID } = cloud.getWXContext();
      console.log(`[uploadShippingFeesXlsx] admin=${OPENID || 'web'} total=${rows.length} applied=${applied} skipped=${rows.length - applied}`);
    }

    // 内部字段 _mongoId 不返客户端
    const out = rows.map(({ _mongoId, ...rest }) => rest);

    return { code: 0, rows: out, summary };
  } catch (err) {
    return { code: err.code || 500, message: err.message || 'error' };
  }
};
```

#### Step 6: 跑 test 验证 pass

```bash
node local-backend/api/test-shim.js 2>&1 | tail -5
```
Expected: 55 passed / 0 failed(51 + 4 new)

#### Step 7: Commit

```bash
git add cloudfunctions/_admin/uploadShippingFeesXlsx/ local-backend/api/test-shim.js
git commit -m "feat(_admin): uploadShippingFeesXlsx 云函数 — 批量上传 Excel 设运费

入参 {xlsxBase64, dryRun},输出 rows + summary。
4 列中文 header 严格匹配(顺序可变);500 行/2MB 上限。

每行 5 种状态:matched / not_found / already_paid / invalid / duplicate_in_file
- 文件内重复:第一次正常,后续标 duplicate
- 已付订单拒改 (沿用 setShippingFee 同保护)
- per-row update,一行失败不影响其他

courierName 列 xlsx 没有(只 4 列),写入时留 null。admin 后续在单笔
OrderDetail 手动补充。

test-shim.js 加 makeTestXlsx helper + 4 测试 (51 → 55 passing)。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase B — 前端 web-admin(4 tasks)

### Task B1: utils/shippingTemplate.ts(浏览器端 xlsx 生成)

**Files:**
- Create: `web-admin/src/utils/shippingTemplate.ts`

- [ ] **Step 1: 创建文件**

`web-admin/src/utils/shippingTemplate.ts`:

```ts
import ExcelJS from 'exceljs';

export async function downloadShippingTemplate(): Promise<void> {
  const wb = new ExcelJS.Workbook();
  const ws = wb.addWorksheet('运费');
  ws.addRow(['订单号', '实际总重量', '应补尾款', '快递单号']);
  ws.addRow(['MG2026051820003932AA07', 2.5, 35.00, 'SF1234567890']);
  ws.getRow(1).font = { bold: true };
  ws.columns = [
    { width: 28 },
    { width: 12 },
    { width: 12 },
    { width: 22 },
  ];
  const buf = await wb.xlsx.writeBuffer();
  const blob = new Blob([buf], {
    type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
  });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = '运费上传模板.xlsx';
  document.body.appendChild(a);
  a.click();
  document.body.removeChild(a);
  URL.revokeObjectURL(url);
}
```

- [ ] **Step 2: tsc check**

```bash
cd web-admin && npx tsc --noEmit 2>&1 | head
```

Expected: 0 errors

- [ ] **Step 3: Commit**

```bash
git add web-admin/src/utils/shippingTemplate.ts
git commit -m "feat(web-admin): shippingTemplate util 浏览器端生成模板 xlsx

4 列 header (订单号/实际总重量/应补尾款/快递单号) + 1 行示例。
点击下载触发 blob.download,无后端往返。

供 BatchShippingFeeModal 调用。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task B2: api/order.ts 加 types + caller

**Files:**
- Modify: `web-admin/src/api/order.ts`

- [ ] **Step 1: 文件末尾追加 types + caller**

```ts
export type BatchShippingStatus =
  | 'matched'
  | 'not_found'
  | 'already_paid'
  | 'invalid'
  | 'duplicate_in_file';

export interface BatchShippingRow {
  row: number;
  orderNo: string;
  weight: number | null;
  fee: number | null;      // cents
  courierNo: string | null;
  status: BatchShippingStatus;
  message?: string;
  before?: {
    shippingFee?: { amount?: number; payStatus?: string; outTradeNo?: string };
    tracking?: { weight?: number; courierName?: string | null; courierNo?: string };
  };
}

export interface BatchShippingResp {
  code: 0;
  rows: BatchShippingRow[];
  summary: {
    matched: number;
    notFound: number;
    alreadyPaid: number;
    invalid: number;
    duplicateInFile: number;
    applied?: number;
  };
}

export async function uploadShippingFeesXlsx(xlsxBase64: string, dryRun: boolean) {
  return callCloud<BatchShippingResp>('_admin/uploadShippingFeesXlsx', { xlsxBase64, dryRun });
}
```

- [ ] **Step 2: tsc check**

```bash
cd web-admin && npx tsc --noEmit 2>&1 | head
```

- [ ] **Step 3: Commit**

```bash
git add web-admin/src/api/order.ts
git commit -m "feat(web-admin/api): uploadShippingFeesXlsx caller + 类型

BatchShippingRow + BatchShippingResp 类型对齐后端 cf 出参。
caller 签名 uploadShippingFeesXlsx(base64, dryRun) → Promise<BatchShippingResp>。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task B3: BatchShippingFeeModal.tsx 组件

**Files:**
- Create: `web-admin/src/pages/BatchShippingFeeModal.tsx`

- [ ] **Step 1: 创建组件**

`web-admin/src/pages/BatchShippingFeeModal.tsx`:

```tsx
import { useState } from 'react';
import { Modal, Button, Upload, Table, Tag, message, Space, Result } from 'antd';
import { DownloadOutlined, InboxOutlined } from '@ant-design/icons';
import type { UploadFile, UploadProps } from 'antd';
import {
  uploadShippingFeesXlsx,
  type BatchShippingRow,
  type BatchShippingResp,
  type BatchShippingStatus,
} from '../api/order';
import { downloadShippingTemplate } from '../utils/shippingTemplate';

interface Props {
  open: boolean;
  onClose: () => void;
  /** 成功 apply 后通知父组件刷新订单列表 */
  onApplied?: () => void;
}

type Phase = 'pick' | 'preview' | 'result';

const STATUS_TAG: Record<BatchShippingStatus, { color: string; label: string }> = {
  matched: { color: 'success', label: '✅ 匹配' },
  not_found: { color: 'error', label: '❌ 未找到' },
  already_paid: { color: 'warning', label: '🟠 已付' },
  invalid: { color: 'error', label: '⚠️ 非法' },
  duplicate_in_file: { color: 'error', label: '🚫 文件重复' },
};

async function fileToBase64(file: File): Promise<string> {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = () => {
      const result = reader.result as string;
      const base64 = result.split(',')[1] || result;
      resolve(base64);
    };
    reader.onerror = reject;
    reader.readAsDataURL(file);
  });
}

export default function BatchShippingFeeModal({ open, onClose, onApplied }: Props) {
  const [phase, setPhase] = useState<Phase>('pick');
  const [fileList, setFileList] = useState<UploadFile[]>([]);
  const [base64, setBase64] = useState<string>('');
  const [resp, setResp] = useState<BatchShippingResp | null>(null);
  const [loading, setLoading] = useState(false);

  const reset = () => {
    setPhase('pick');
    setFileList([]);
    setBase64('');
    setResp(null);
    setLoading(false);
  };

  const handleClose = () => {
    reset();
    onClose();
  };

  const handleNext = async () => {
    if (!fileList[0]?.originFileObj) {
      message.error('请选 xlsx 文件');
      return;
    }
    setLoading(true);
    try {
      const b64 = await fileToBase64(fileList[0].originFileObj as File);
      setBase64(b64);
      const r = await uploadShippingFeesXlsx(b64, true);
      setResp(r);
      setPhase('preview');
    } catch (e: any) {
      message.error(e.message || '上传解析失败');
    } finally {
      setLoading(false);
    }
  };

  const handleApply = async () => {
    setLoading(true);
    try {
      const r = await uploadShippingFeesXlsx(base64, false);
      setResp(r);
      setPhase('result');
      onApplied?.();
    } catch (e: any) {
      message.error(e.message || '应用失败');
    } finally {
      setLoading(false);
    }
  };

  const uploadProps: UploadProps = {
    accept: '.xlsx,.xls',
    maxCount: 1,
    fileList,
    beforeUpload: () => false, // 不自动上传,我们手动 fileToBase64
    onChange: ({ fileList: fl }) => setFileList(fl.slice(-1)),
  };

  const columns = [
    { title: '行', dataIndex: 'row', width: 50 },
    { title: '订单号', dataIndex: 'orderNo', width: 200 },
    {
      title: '运费',
      dataIndex: 'fee',
      width: 80,
      render: (v: number | null) => (v != null ? `¥${(v / 100).toFixed(2)}` : '—'),
    },
    {
      title: '重量',
      dataIndex: 'weight',
      width: 70,
      render: (v: number | null) => (v != null ? `${v} kg` : '—'),
    },
    { title: '单号', dataIndex: 'courierNo', width: 140 },
    {
      title: '状态',
      dataIndex: 'status',
      width: 110,
      render: (s: BatchShippingStatus, row: BatchShippingRow) => (
        <Space direction="vertical" size={2}>
          <Tag color={STATUS_TAG[s].color}>{STATUS_TAG[s].label}</Tag>
          {row.message && <span style={{ fontSize: 11, color: '#999' }}>{row.message}</span>}
        </Space>
      ),
    },
    {
      title: '当前数据(会被覆盖)',
      key: 'before',
      render: (_: any, row: BatchShippingRow) => {
        if (!row.before) return '—';
        const parts: string[] = [];
        if (row.before.shippingFee?.amount != null) {
          parts.push(`运费 ¥${(row.before.shippingFee.amount / 100).toFixed(2)} (${row.before.shippingFee.payStatus})`);
        }
        if (row.before.tracking?.courierNo) {
          parts.push(`单号 ${row.before.tracking.courierNo}`);
        }
        return parts.length ? parts.join(' · ') : '—';
      },
    },
  ];

  return (
    <Modal
      title="Excel 批量上传运费"
      open={open}
      onCancel={handleClose}
      footer={null}
      width={phase === 'preview' ? 1100 : 600}
      destroyOnClose
    >
      {phase === 'pick' && (
        <Space direction="vertical" size={16} style={{ display: 'flex', width: '100%' }}>
          <div>
            <Button icon={<DownloadOutlined />} onClick={downloadShippingTemplate}>
              下载模板
            </Button>
            <span style={{ marginLeft: 12, color: '#666', fontSize: 13 }}>
              4 列中文 header:订单号 / 实际总重量 / 应补尾款 / 快递单号
            </span>
          </div>
          <Upload.Dragger {...uploadProps}>
            <p className="ant-upload-drag-icon"><InboxOutlined /></p>
            <p className="ant-upload-text">点击或拖入 xlsx 文件</p>
            <p className="ant-upload-hint">单次最多 500 行 / 2MB</p>
          </Upload.Dragger>
          <Space style={{ justifyContent: 'flex-end', width: '100%' }}>
            <Button onClick={handleClose}>关闭</Button>
            <Button type="primary" loading={loading} onClick={handleNext} disabled={!fileList.length}>
              下一步:预览
            </Button>
          </Space>
        </Space>
      )}

      {phase === 'preview' && resp && (
        <Space direction="vertical" size={12} style={{ display: 'flex', width: '100%' }}>
          <div>
            共 <strong>{resp.rows.length}</strong> 行 —{' '}
            ✅ 匹配 {resp.summary.matched} ·{' '}
            ❌ 未找到 {resp.summary.notFound} ·{' '}
            🟠 已付 {resp.summary.alreadyPaid} ·{' '}
            🚫 重复 {resp.summary.duplicateInFile} ·{' '}
            ⚠️ 非法 {resp.summary.invalid}
          </div>
          <Table
            dataSource={resp.rows}
            columns={columns}
            rowKey="row"
            size="small"
            pagination={false}
            scroll={{ y: 400 }}
          />
          <Space style={{ justifyContent: 'flex-end', width: '100%' }}>
            <Button onClick={() => setPhase('pick')}>返回选文件</Button>
            <Button
              type="primary"
              loading={loading}
              onClick={handleApply}
              disabled={resp.summary.matched === 0}
            >
              ✅ 确认应用 {resp.summary.matched} 行
            </Button>
          </Space>
        </Space>
      )}

      {phase === 'result' && resp && (
        <Result
          status="success"
          title={`成功应用 ${resp.summary.applied ?? 0} 条`}
          subTitle={
            resp.summary.applied !== resp.rows.length
              ? `跳过 ${resp.rows.length - (resp.summary.applied ?? 0)} 条(详情见上一步预览)`
              : '全部应用成功'
          }
          extra={<Button type="primary" onClick={handleClose}>关闭</Button>}
        />
      )}
    </Modal>
  );
}
```

- [ ] **Step 2: tsc check**

```bash
cd web-admin && npx tsc --noEmit 2>&1 | head
```

- [ ] **Step 3: Commit**

```bash
git add web-admin/src/pages/BatchShippingFeeModal.tsx
git commit -m "feat(web-admin): BatchShippingFeeModal 3-phase 组件

phase 1 选文件 (Upload.Dragger + 下载模板按钮)
phase 2 预览 (antd Table 渲染 rows,带 before 列显示会被覆盖的数据,
        summary 横条,确认应用 N 行)
phase 3 结果 (Result success + 跳过统计)

文件 → base64 用 FileReader。
dryRun 调 cf 两次 (预览 + 应用),共享 base64 不重传文件。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task B4: Orders.tsx 顶部按钮 + modal 集成

**Files:**
- Modify: `web-admin/src/pages/Orders.tsx`

- [ ] **Step 1: 找现有 toolbar / state hooks**

```bash
grep -n "useState\|^export\|return\s*(" web-admin/src/pages/Orders.tsx | head -20
```

观察现有 toolbar 在哪(toolbar 一般在 Table 上方,有 reload / filter 按钮),以及 reload 函数名(load 或 fetchOrders 等)。

- [ ] **Step 2: 加 import + state + 按钮**

文件顶部 import 加:
```tsx
import { UploadOutlined } from '@ant-design/icons';
import BatchShippingFeeModal from './BatchShippingFeeModal';
```

组件内加 state(找现有 useState 的位置追加):
```tsx
const [batchModalOpen, setBatchModalOpen] = useState(false);
```

在现有 toolbar 末尾(刷新按钮旁)加:
```tsx
<Button icon={<UploadOutlined />} onClick={() => setBatchModalOpen(true)}>
  Excel 批量运费
</Button>
```

在 return 的合适位置(toolbar / Table 之外,可以放在 root fragment 内最后)加 modal:
```tsx
<BatchShippingFeeModal
  open={batchModalOpen}
  onClose={() => setBatchModalOpen(false)}
  onApplied={load}  // 替换成 Orders.tsx 实际的 reload 函数名
/>
```

> 关键:`onApplied` 接的回调要是 Orders.tsx 现有的"刷新订单列表"函数。如果实际叫 `loadOrders` / `fetchOrders`,改用对应名。

- [ ] **Step 3: tsc check**

```bash
cd web-admin && npx tsc --noEmit 2>&1 | head
```

- [ ] **Step 4: Commit**

```bash
git add web-admin/src/pages/Orders.tsx
git commit -m "feat(web-admin/Orders): toolbar 加 Excel 批量运费 按钮

点击弹 BatchShippingFeeModal。
apply 成功后调 onApplied 触发 Orders reload,反映新 shippingFee + tracking。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase C — 部署 + E2E 验证(1 task)

### Task C1: push + deploy + 手测

- [ ] **Step 1: push 触发 CI**

```bash
git push
gh run watch $(gh run list --workflow="Deploy App" --limit 1 --json databaseId -q '.[0].databaseId') --exit-status
```

Expected: deploy success(test-shim 55 passing,build ok)

- [ ] **Step 2: 准备测试 Excel**

桌面打开 Excel 或 Numbers,4 列 header:

| 订单号 | 实际总重量 | 应补尾款 | 快递单号 |
|---|---|---|---|
| MG2026051820003932AA07 | 2.0 | 5.50 | SF99999 |
| MG_NONEXIST_TEST | 1.0 | 3.00 | XX |

保存 .xlsx。

(或者直接在 admin modal 里点"下载模板",改改值再用。)

- [ ] **Step 3: 浏览器测**

```
1. admin /admin/orders → 顶部"Excel 批量运费"按钮
2. modal 弹开 → "下载模板" 验证下载成功 + 4 列正确
3. 选刚才准备的 xlsx → "下一步:预览"
4. 预览页:
   ✓ 第一行:✅ 匹配 + 会覆盖原 shippingFee(若有)
   ✓ 第二行:❌ 未找到
   ✓ 顶部 summary 1 匹配 1 未找到
5. 点 "✅ 确认应用 1 行"
6. 结果页:成功应用 1 / 跳过 1
7. 关闭 → Orders 列表 reload,看 MG...AA07 的 shippingFee 变成新值
```

- [ ] **Step 4: 用 mongo 二次验证**

```bash
ssh -i ~/.ssh/mogu_deploy ubuntu@43.159.198.145 'sudo docker exec mogu_mongo mongosh --quiet --eval "
  const d = db.getSiblingDB(\"mogu_express\");
  d.orders.find({orderNo: \"MG2026051820003932AA07\"}, {orderNo:1, shippingFee:1, tracking:1}).forEach(printjson);
"'
```

期望:
- `shippingFee.amount: 550`(¥5.50)
- `shippingFee.payStatus: 'pending'`
- `shippingFee.outTradeNo` 新的 SHIP<...>
- `tracking.weight: 2`
- `tracking.courierNo: 'SF99999'`
- `tracking.courierName: null`(Excel 没此列)

- [ ] **Step 5: 全 OK 后无需 commit(测试不写代码)。报告状态收尾。

---

## Self-Review

### Spec 覆盖

| Spec 部分 | Plan task |
|---|---|
| 后端 cf uploadShippingFeesXlsx 全实现 | A1 |
| 校验:header / 行数 / 单 row 字段 | A1 (classifyRow + header check) |
| 5 种 row status | A1 (test 1 + impl) |
| dryRun 预览 vs commit 应用 | A1 (test 1 + 2 + impl) |
| courierName 留 null | A1 (impl + test 2 断言) |
| outTradeNo SHIP 前缀 | A1 (impl) |
| 鉴权 admin only | A1 (test 3) |
| 模板下载 | B1 |
| Modal 3 phase | B3 |
| 预览 before 列 | B3 (columns) |
| Orders 顶部按钮 | B4 |
| 测试 4 个(dryRun/apply/auth/header) | A1 |

无 gap。

### Placeholder scan

- B4 "找现有 toolbar 位置" — 引导但要 reader 自己看;现有代码我没看完。具体位置 follow grep 输出 + 现有风格。✓ 可接受(实施者要熟悉 Orders.tsx)
- B4 onApplied="load" 但实际函数名未知 — 给提示让 reader 自己改对名。✓ 可接受
- A1 测试 2 修正 fee 单位说明 — 已显式标明"Excel 列填元,后端转 cents"。✓
- 其他步骤均有完整代码块。

### Type consistency

- `BatchShippingRow` shape 在 A1 cf 实现 + B2 types + B3 modal 使用,字段一致(row, orderNo, weight, fee, courierNo, status, message?, before?)。✓
- `BatchShippingStatus` 5 个值贯穿一致。✓
- `summary` shape:cf impl + B2 types + B3 modal display 一致。✓
- `BatchShippingResp.summary.applied` optional(dryRun=true 时缺,dryRun=false 时填)— cf 实现 + types + modal display 一致。✓

---

## 估算

| Phase | Tasks | 估算 |
|---|---|---|
| A 后端 cf + tests | 1 task | ~2.5 h |
| B 前端 web-admin | 4 tasks(util / api / modal / Orders 集成) | ~3 h |
| C 部署 + 验证 | 1 task | ~30 min |
| **合计** | **6 tasks** | **~6 h focused** |

预计 1 天(含 review iterations、subagent 开销)。
