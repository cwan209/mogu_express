.hero {
  padding: 24rpx 8rpx;
  margin-bottom: 16rpx;
}
.t { font-size: 34rpx; font-weight: 700; color: var(--text-primary); }
.sub { color: var(--text-secondary); font-size: 24rpx; margin-top: 6rpx; }

.form {
  background: var(--bg-card);
  border-radius: var(--radius-md);
  overflow: hidden;
  margin-bottom: 32rpx;
}

.tip {
  color: var(--text-placeholder);
  font-size: 22rpx;
  text-align: center;
  margin-top: 16rpx;
}
